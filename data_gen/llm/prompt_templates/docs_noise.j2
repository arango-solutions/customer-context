You are writing a routine business document for account management purposes.

FACTS (read-only context — use exactly these; do not invent additional facts):
- Account: {{ account_name }}
- Document type: {{ doc_type }}
- Date: {{ date }}
- Topic: {{ topic }}

INSTRUCTIONS:
Fill ONLY the prose fields in the output schema:
- title: A clear, routine document title.
- executive_summary: 2-3 sentences summarizing the document's routine purpose.
- body_sections: 2-3 paragraphs of routine account documentation. Each section 2-4 sentences.
- action_items: 1-3 routine next steps as short phrases.

ABSOLUTE PROHIBITIONS (do not include any of these in your output):
- Dollar amounts (e.g. "$150,000")
- Dates in YYYY-MM-DD format or "Month DD, YYYY" format
- UUID strings or entity IDs
- The following terms must NOT appear anywhere in your output (these are signal terms for other questions): {% for term in prohibited_terms %}{{ term }}{% if not loop.last %}, {% endif %}{% endfor %}

Write in a professional, routine business document style.
This is a routine, positive document — no escalation, no competitive mentions, no at-risk language.
Do not use markdown headers, asterisks, or bullet characters in prose fields.
