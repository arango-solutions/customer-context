---
phase: 07-grounding-eval-demo-hardening
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - scripts/rehearse.ts
  - scripts/tsconfig.json
autonomous: false
requirements: [EVAL-02]

must_haves:
  truths:
    - "scripts/rehearse.ts fires a few CONCURRENT /api/ask calls against the live deploy and asserts each returns a grounded 200 (degrades gracefully, not a load test)"
    - "The script fires adversarial questions (out-of-scope / privacy / not-in-data) against the live deploy and asserts each REFUSES — never fabricates"
    - "The script can reach the SSO-protected deploy via the automation bypass secret (or is run from the authenticated browser session per D-08)"
  artifacts:
    - path: "scripts/rehearse.ts"
      provides: "Light concurrent + adversarial rehearsal harness against the deployed /api/ask"
      contains: "Promise.all"
      min_lines: 30
    - path: "scripts/tsconfig.json"
      provides: "Minimal tsconfig (extends agent's) so rehearse.ts type-checks with NodeNext resolution of the cross-workspace ../agent/test/*.js import + ESM .js discipline — drives the gate"
      contains: "extends"
  key_links:
    - from: "scripts/rehearse.ts"
      to: "/api/ask"
      via: "concurrent POST fetch"
      pattern: "/api/ask"
---

<objective>
Implement the EVAL-02 rehearsal harness (D-07): a lightweight `scripts/rehearse.ts` that fires a few CONCURRENT `/api/ask` calls plus the adversarial question set (out-of-scope / privacy / not-in-data → MUST refuse, never fabricate) against the REAL Vercel deploy, asserting graceful behavior. This is a rehearsal, NOT a load test.

Purpose: Prove the live path holds up under a few simultaneous demo-realistic questions and that adversarial inputs refuse on the deployed surface (not just in the eval) — the final EVAL-02 demo-hardening gate.

Output: `scripts/rehearse.ts` and `scripts/tsconfig.json` (the dedicated config that makes the type-check a real gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-grounding-eval-demo-hardening/07-CONTEXT.md
@.planning/phases/07-grounding-eval-demo-hardening/07-RESEARCH.md

@web/app/api/ask/route.ts
@agent/test/adversarial.ts
@agent/test/questions.eval.test.ts
@agent/src/envelope.ts
@agent/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/tsconfig.json + scripts/rehearse.ts — concurrent + adversarial live rehearsal</name>
  <files>scripts/tsconfig.json, scripts/rehearse.ts</files>
  <read_first>
    - agent/tsconfig.json (the base to extend: module/moduleResolution = NodeNext, strict, esModuleInterop, skipLibCheck, types:["node"] — rehearse.ts must type-check under the SAME NodeNext/ESM discipline so the `../agent/test/adversarial.js` relative import and `.js` extensions resolve exactly as they would at runtime under tsx)
    - web/app/api/ask/route.ts (the POST contract: JSON `{question}`, returns a UIMessageStream SSE Response — the persistent `data-envelope` part is the grounded answer; read how the body is shaped)
    - agent/test/adversarial.ts (the adversarial question set from Plan 07-01 — reuse it via relative import; do not redefine or copy the questions)
    - agent/test/questions.eval.test.ts (a representative grounded question to fire concurrently, e.g. the Q2 renewal-risk prompt)
    - agent/src/envelope.ts (the Envelope shape to assert against: refused flag, citations[])
    - 07-RESEARCH.md §"Code Examples"→"Light concurrent rehearsal" + the note on the SSO automation bypass secret (VERCEL_AUTOMATION_BYPASS_SECRET) / D-08 browser-session alternative
  </read_first>
  <action>
    FIRST create `scripts/tsconfig.json` so the type-check is a real gate (a bare-file `tsc` runs under loose defaults and won't resolve the cross-workspace relative import or the `.js` ESM convention — there is no root tsconfig). It must `"extends": "../agent/tsconfig.json"` (inheriting NodeNext module/moduleResolution, strict, esModuleInterop, skipLibCheck, types:["node"]), set `"compilerOptions": { "noEmit": true, "rootDir": ".." }` so the cross-workspace `../agent/test/adversarial.ts` and its `.js` ESM imports resolve under NodeNext exactly as tsx will run them, and `"include": ["rehearse.ts"]`. Confirm the inherited `noEmit`/`include` semantics against agent/tsconfig.json at implement time (the base already sets noEmit:true).

    THEN create `scripts/rehearse.ts` (run via tsx, mirroring agent's `tsx` usage). Read `BASE` (the deploy URL) and the SSO automation bypass from env (`VERCEL_AUTOMATION_BYPASS_SECRET` — passed as the `x-vercel-protection-bypass` header or query param to reach the SSO-protected deploy; document the D-08 browser-session alternative in a header comment). Part A (concurrent): fire N=4 simultaneous `POST ${BASE}/api/ask` with a real grounded question (e.g. the Q2 renewal-risk prompt) via `Promise.all`; for each response assert `r.ok` (200) and that the streamed body yields a non-refused envelope with `citations.length > 0` (parse the UIMessageStream `data-envelope` part — read how /api/ask streams it). Part B (adversarial): import the adversarial set with the RELATIVE path `import { ... } from '../agent/test/adversarial.js'` — this is the ONLY working path (the `customer360-agent` package `exports` map exposes only `.` and `./stream`, NOT `test/`), and it keeps the set DRY with Plan 07-01; do NOT copy the array (a copy would silently diverge from Plan 01's adversarial set). Fire each against `/api/ask` and assert the resulting envelope has `refused === true` with no fabricated `_id` (every citation `_id` matches the `Collection/key` shape). Print a clear PASS/FAIL summary per call and exit non-zero on any failure so it is a usable gate. This is a FEW calls (rehearsal), explicitly NOT a load test — do not ramp concurrency. No fenced code in the plan; confirm the UIMessageStream envelope-extraction approach against /api/ask + the AI SDK stream format at implement time.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p scripts/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/tsconfig.json` exists and `extends` `../agent/tsconfig.json` (NodeNext resolution inherited).
    - `npx tsc --noEmit -p scripts/tsconfig.json` exits 0 — and its exit code is the gate (no masking pipe, no trailing presence check); any type error (including an unresolved cross-workspace import or a missing `.js` extension) fails it.
    - `scripts/rehearse.ts` imports the adversarial set via the relative path `../agent/test/adversarial.js` — NOT a copied array (HARD requirement; the package exports map does not expose `test/`).
    - `scripts/rehearse.ts` fires N=4 concurrent grounded `/api/ask` calls (asserting grounded 200, non-refused, cited) + the adversarial set (asserting `refused === true`, no fabricated `_id`).
    - The script reads `VERCEL_AUTOMATION_BYPASS_SECRET` from env (never hard-coded) and documents the D-08 browser-session alternative.
    - Prints a per-call PASS/FAIL summary and exits non-zero on any failure.
  </acceptance_criteria>
  <done>`scripts/tsconfig.json` extends the agent tsconfig and makes `npx tsc --noEmit -p scripts/tsconfig.json` a real pass/fail gate; `scripts/rehearse.ts` type-checks under it, imports the adversarial set via the `../agent/test/adversarial.js` relative path (not copied), fires N concurrent grounded `/api/ask` calls (asserting grounded 200) + the adversarial set (asserting refusal, no fabricated `_id`), handles the SSO bypass, prints a per-call PASS/FAIL summary, and exits non-zero on failure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>scripts/rehearse.ts — the live concurrent + adversarial rehearsal harness (Task 1).</what-built>
  <how-to-verify>
    1. Ensure the deploy is warm (manual pre-warm: hit /api/canary once, or wait for a cron cycle).
    2. Run `BASE=<deploy-url> VERCEL_AUTOMATION_BYPASS_SECRET=<secret> npx tsx scripts/rehearse.ts` (or run from the authenticated browser session per D-08 if not using the bypass secret).
    3. Confirm: all N concurrent grounded questions return 200 with a non-refused, cited envelope (degrade gracefully — no 504/500s).
    4. Confirm: every adversarial question REFUSES (refused===true) with no fabricated `_id`.
    5. Confirm the script exits 0 on success / non-zero if any assertion failed.
  </how-to-verify>
  <resume-signal>Type "approved" with the rehearsal result (N concurrent grounded + adversarial refusals all passed), or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rehearse script → SSO-protected deploy | Script must hold the automation bypass secret (or run in the authenticated browser session) to reach the protected /api/ask |
| concurrent callers → /api/ask | A few simultaneous requests against the live agent surface (rehearsal, not a load test) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information Disclosure | VERCEL_AUTOMATION_BYPASS_SECRET handling in the rehearsal script | mitigate | Secret read from env only, never hard-coded or logged; script is a dev/rehearsal tool, not deployed; D-08 browser-session alternative documented. |
| T-07-09 | Denial of Service | concurrent /api/ask rehearsal hammering the live agent | accept | Deliberately a FEW calls (N=4), not a ramp; bounded by the agent's `stepCountIs(12)` + `maxDuration=60`; presenter-run, not automated/continuous. |
| AR-06-2 | Denial of Service | `/api/ask` per-IP rate-limit | accept (carried forward, D-09) | Unchanged; the rehearsal is explicitly NOT a load test and does not change the deferral. |
</threat_model>

<verification>
- `npx tsc --noEmit -p scripts/tsconfig.json` exits 0 (the type-check IS the gate; the dedicated config resolves the cross-workspace relative import + `.js` ESM discipline under NodeNext).
- Live run (manual checkpoint): N concurrent grounded questions return 200 + cited envelopes; adversarial questions refuse with no fabricated `_id`; exit code reflects pass/fail.
</verification>

<success_criteria>
- A lightweight rehearsal harness firing a few concurrent grounded `/api/ask` calls + the adversarial set (imported, not copied) against the real deploy.
- A dedicated `scripts/tsconfig.json` so the type-check is a real pass/fail gate, not a presence check.
- Concurrent grounded calls degrade gracefully (all 200, cited); adversarial calls refuse with no fabricated `_id`.
- Reaches the SSO-protected deploy via the automation bypass secret (or documented browser-session path, D-08).
- Exits non-zero on any failure so it is a usable pre-demo gate.
</success_criteria>

<output>
Create `.planning/phases/07-grounding-eval-demo-hardening/07-03-SUMMARY.md` when done.
</output>
