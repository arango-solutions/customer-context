---
phase: 10-answer-provenance-edge-enrichment
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - agent/src/tools/structuredQuery.ts
  - agent/test/structuredQuery.test.ts
  - agent/src/agent.ts
  - agent/src/stream.ts
autonomous: false
requirements: [VIZ-01]

must_haves:
  truths:
    - "structuredQuery synthesizes account-anchored structural edges (Account node → each cited record), kind:'structural', label:'account', NEVER 'traversed' (SC-4, D-02)"
    - "synthesized structural edge ids are deterministic (`structural:${accountId}:${record._id}`), no uuid4/random"
    - "the account-facet self-edge (_to === _from) is skipped"
    - "the returnedIds set in agent.ts AND stream.ts is built ONLY from frag._ids — never from edges (SC-5 isolation, verified unchanged)"
    - "the eval gate stays GREEN — edge enrichment is additive (SC-5)"
  artifacts:
    - path: "agent/src/tools/structuredQuery.ts"
      provides: "buildPath synthesizes kind:'structural' edges deterministically"
      contains: "structural"
    - path: "agent/test/structuredQuery.test.ts"
      provides: "structural-edge determinism + never-traversed + self-edge-skip assertions"
    - path: "agent/src/agent.ts"
      provides: "returnedIds-from-_ids-only with an explicit edges-are-not-grounding-anchors comment"
    - path: "agent/src/stream.ts"
      provides: "returnedIds-from-_ids-only with the same comment"
  key_links:
    - from: "agent/src/tools/structuredQuery.ts"
      to: "agent/src/envelope.ts"
      via: "buildPath returns edges: RetrievalPathEdge[]"
      pattern: "kind: 'structural'"
    - from: "agent/src/agent.ts"
      to: "agent/src/grounding.ts"
      via: "returnedIds built only from frag._ids feeds enforceGrounding"
      pattern: "returnedIds.add"
---

<objective>
Synthesize the honest structured-side representation and lock the SC-5 isolation guarantee. structuredQuery walks no edges (account-scoped attribute lookups), so per D-02 it SYNTHESIZES account-anchored structural edges — explicitly kind:'structural', never 'traversed'. Then prove (and comment-guard) that edge _ids never enter the grounding returnedIds set in either agent loop, and that the locked eval gate stays GREEN.

Purpose: SC-4 (structured cluster as an honest account-induced subgraph) + SC-5 (additivity — no grounding/eval regression). This is the load-bearing honesty boundary on the structured side and the additivity proof for the whole phase.

Output: deterministic structural edges in structuredQuery's buildPath; an explicit isolation comment + verified-unchanged returnedIds loops in agent.ts/stream.ts; eval gate GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-answer-provenance-edge-enrichment/10-CONTEXT.md
@.planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md
@.planning/phases/10-answer-provenance-edge-enrichment/10-VALIDATION.md
@.planning/phases/10-answer-provenance-edge-enrichment/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: structuredQuery — synthesize deterministic kind:'structural' account edges (D-02 / SC-4)</name>
  <files>agent/src/tools/structuredQuery.ts, agent/test/structuredQuery.test.ts</files>
  <read_first>
    - agent/src/tools/structuredQuery.ts (the file being modified — buildPath lines 45-56 currently takes (collection, data, query); the six facet callers pass through runFacet; accountId is in scope at every call site)
    - agent/src/envelope.ts (RetrievalPathEdge from 10-01)
    - .planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md (§"SC-4 / D-02: structuredQuery" exact buildPath diff — thread accountId, _from = `Account/${accountId}` real node, _to = row._id, _id = `structural:${accountId}:${row._id}` deterministic clearly-synthetic, collection 'account', kind 'structural', label 'account'; §"Open Questions Q2" skip the self-edge where _to === _from; §A2 Account._key == account_id per project memory)
    - agent/test/structuredQuery.test.ts (extend; existing facet tests + canRun live guard pattern)
  </read_first>
  <behavior>
    - buildPath threads accountId and produces one structural edge per data row
    - every synthesized edge has kind:'structural', label:'account', collection:'account' — NEVER kind:'traversed'
    - edge _id is exactly `structural:${accountId}:${row._id}` (deterministic; same inputs → same id; no random)
    - edge _from is `Account/${accountId}` and _to is the row's _id
    - the account-facet row whose _to === _from (Account/x → Account/x) is skipped (no degenerate self-edge)
    - the existing _ids/graph/collection/query on the fragment are unchanged; the planner-read data rows are unchanged
  </behavior>
  <action>
    Change buildPath's signature to (collection, data, query, accountId) and update all six runFacet call sites to pass accountId. In buildPath, after computing _ids, build edges = data.map(d => ({ _id: `structural:${accountId}:${d._id}`, _from: `Account/${accountId}`, _to: d._id as string, collection: 'account', kind: 'structural' as const, label: 'account' as const })) then filter out any edge where _to === _from (the account facet's self-row). Return edges on the fragment alongside the unchanged graph/collection/_ids/query. Do NOT use uuid4/Math.random (D-02 determinism). Add a code comment that the `structural:` id is a deterministic synthetic marker, NOT an ArangoDB document key, and that kind:'structural' signals "account-induced, not a traversal that ran" (the D-02 honesty line). In agent/test/structuredQuery.test.ts add a describe block (pure where possible — buildPath can be tested by exercising runFacet under canRun, or by extracting/importing buildPath if exported) asserting the six behaviors; specifically assert NO synthesized edge has kind === 'traversed' and that re-running with the same inputs yields identical edge _ids (determinism).
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/structuredQuery.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - structuredQuery.ts buildPath synthesizes edges with kind:'structural', label:'account', _from `Account/${accountId}`, _id `structural:${accountId}:${record._id}`
    - no synthesized edge has kind === 'traversed' (asserted in the test)
    - the self-edge where _to === _from is filtered out
    - edge ids are deterministic (no uuid4/Math.random; test asserts identical ids on repeat)
    - cd agent && npx vitest run test/structuredQuery.test.ts exits 0
    - cd agent && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>structuredQuery synthesizes deterministic kind:'structural' account edges, skips the self-edge, never emits 'traversed'; test proves it; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Lock SC-5 isolation in both agent loops + prove eval-gate additivity</name>
  <files>agent/src/agent.ts, agent/src/stream.ts</files>
  <read_first>
    - agent/src/agent.ts (the returnedIds loop ~line 321-335: `for (const id of frag._ids) returnedIds.add(id)` — this MUST stay built ONLY from frag._ids; mergeRetrievalPaths call ~line 345)
    - agent/src/stream.ts (the near-duplicate returnedIds loop ~line 109-122 + same mergeRetrievalPaths call ~line 130 + enforceGrounding ~line 134)
    - agent/src/grounding.ts (enforceGrounding reads citations[]._id vs returnedIds; never reads edges — confirms additivity)
    - .planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md (§"The isolation guarantee (SC-5)"; §"Pitfall 3 Edge _ids leaking into grounding" — do NOT add edges to returnedIds; add an explicit comment; §"Pitfall 4 streaming path" — edges ride inside the fragment so both loops carry them for free via mergeRetrievalPaths)
    - .planning/phases/10-answer-provenance-edge-enrichment/10-CONTEXT.md (D-06 LOCKED: never edit scripts/eval-gate.ts or agent/test/questions.eval.test.ts to pass)
  </read_first>
  <action>
    Do NOT change the logic of either returnedIds loop — they already build returnedIds ONLY from frag._ids, which is exactly the SC-5 requirement. Add a single explicit comment immediately above the `for (const id of frag._ids) returnedIds.add(id)` line in BOTH agent.ts and stream.ts stating that edges are provenance-only (Phase 11 VIZ-02) and must NEVER be added to returnedIds — an edge _id is not a citable grounding anchor (Pitfall 3 / SC-5). Confirm by reading that fragments (carrying edges[] after 10-01/10-02/Task-1) flow through the SAME mergeRetrievalPaths in both files, so edges reach the envelope on both the non-streaming and streaming paths with no further change. Make NO edit to scripts/eval-gate.ts or agent/test/questions.eval.test.ts (D-06 LOCKED). Then run the full unit suite and the locked eval gate to prove additivity (SC-5): the gate must report GREEN with the same verdict as before enrichment.
  </action>
  <verify>
    <automated>cd agent && npx vitest run && cd .. && npx tsx scripts/eval-gate.ts</automated>
  </verify>
  <acceptance_criteria>
    - agent.ts returnedIds loop is unchanged except for an added comment; it still adds ONLY frag._ids
    - stream.ts returnedIds loop is unchanged except for the same comment; it still adds ONLY frag._ids
    - grep confirms no `returnedIds.add(` is fed an edge value in either file (returnedIds built only from frag._ids): `grep -v '^[[:space:]]*//' agent/src/agent.ts agent/src/stream.ts | grep -c 'edge.*returnedIds\|returnedIds.*edge'` returns 0
    - git diff is empty for scripts/eval-gate.ts and agent/test/questions.eval.test.ts (D-06 intact)
    - cd agent && npx vitest run exits 0
    - npx tsx scripts/eval-gate.ts reports GREEN (exit 0, SC-5 additivity proven)
  </acceptance_criteria>
  <done>Both returnedIds loops carry the isolation comment and remain _ids-only; edges flow to the envelope via mergeRetrievalPaths on both paths; full unit suite green; eval gate GREEN with D-06 untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Streaming-path edges[] smoke-test (human-verify)</name>
  <action>Pause for human verification — present the streaming smoke-test steps below and wait for the resume signal. No code is written in this task; it confirms the enrichment from Tasks 1-2 reaches the streaming envelope (the eval gate covers only the non-streaming path).</action>
  <what-built>Edge enrichment now flows through the STREAMING path (stream.ts) as well as the non-streaming path. The eval gate only exercises the non-streaming path (project memory: agent-loop-shared-factory), so the streaming envelope's edges[] must be smoke-tested by a human per VALIDATION.md's Manual-Only Verifications.</what-built>
  <how-to-verify>
    1. Start the web app (the deployed streaming UI, or `cd web && npm run dev` if running locally).
    2. Ask a dual-graph question that exercises both tools, e.g. the Q12 Meridian reconciliation (usage green vs sentiment red).
    3. Inspect the streamed answer envelope's retrievalPath[].edges[] (via the network response or a UI debug panel).
    4. Confirm: edges[] is present and non-empty; PART_OF and same_as edges are labeled kind:'traversed'; the structured cluster shows kind:'structural' label:'account' edges; the hybrid question→chunk edges show kind:'hybrid'; and NO edge is mislabeled 'traversed' for the structured side.
    Expected outcome: the streamed envelope carries honestly-labeled edges[] identical in shape to the non-streaming path.
  </how-to-verify>
  <resume-signal>Type "approved" if the streamed envelope carries honestly-labeled edges[], or describe what is missing/mislabeled.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| structuredQuery rows → synthesized structural edges | honesty boundary: an account-induced set must NEVER be drawn as a traversal that ran |
| fragment edges[] → grounding returnedIds | additivity boundary (SC-5) — the load-bearing isolation rule for this plan |

This plan synthesizes structural edges in pure TS (no AQL change, no DB write) and adds documentation/comments to the agent loops. No new external input, no bind values, no auth/PII surface. The two real threats are the honesty/fabrication threat (structural mislabeled traversed) and the grounding-leak threat. No web-app padding.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering/Repudiation (confident-wrong-graph) | structuredQuery synthesized edges | mitigate | Synthesized edges are hard-coded kind:'structural', label:'account', with clearly-synthetic `structural:` ids — never 'traversed'. The test asserts NO synthesized edge has kind === 'traversed', and the D-04 guard (10-01) treats structural as exempt because it is never claimed as a traversal. This is the structured-side half of the project's core honesty bar. |
| T-10-07 | Information Disclosure (grounding leak) | agent.ts/stream.ts returnedIds | mitigate | returnedIds is built ONLY from frag._ids in both loops; this plan adds an explicit comment forbidding edge _ids and changes no logic. The grep acceptance check confirms no edge feeds returnedIds; the eval gate staying GREEN (SC-5) confirms groundingScore is unshifted. |
| T-10-08 | Repudiation (gate tampering to pass) | scripts/eval-gate.ts, questions.eval.test.ts | mitigate | D-06 LOCKED — the acceptance criteria require an EMPTY git diff on both files; the gate must go GREEN on merit (additive enrichment), never by loosening thresholds. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `cd agent && npx vitest run test/structuredQuery.test.ts` exits 0 (structural-edge determinism + never-traversed)
- `cd agent && npx vitest run` exits 0 (full unit suite — per-wave merge gate)
- `npx tsx scripts/eval-gate.ts` reports GREEN (SC-5 additivity; D-06 intact)
- `git diff --quiet scripts/eval-gate.ts agent/test/questions.eval.test.ts` (no gate edits)
- Human streaming smoke-test approved (VALIDATION.md Manual-Only Verifications)
</verification>

<success_criteria>
- structuredQuery synthesizes deterministic kind:'structural' account edges, self-edge skipped, never 'traversed' (SC-4, D-02)
- Both agent loops keep returnedIds built only from frag._ids with an explicit isolation comment (SC-5)
- Full unit suite green; eval gate GREEN; D-06 files unedited
- Streaming path carries honestly-labeled edges[] (human-verified)
</success_criteria>

<output>
Create `.planning/phases/10-answer-provenance-edge-enrichment/10-03-SUMMARY.md` when done.
</output>
