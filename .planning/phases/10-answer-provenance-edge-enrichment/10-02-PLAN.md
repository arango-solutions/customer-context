---
phase: 10-answer-provenance-edge-enrichment
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - agent/src/tools/hybridRetrieve.ts
  - agent/src/tools/bridgeResolve.ts
  - agent/test/hybridRetrieve.test.ts
  - agent/test/bridgeResolve.test.ts
autonomous: true
requirements: [VIZ-01]

must_haves:
  truths:
    - "hybridRetrieve returns the real PART_OF edge (Chunk→Document) it traversed as kind:'traversed', label:'PART_OF' (SC-1)"
    - "hybridRetrieve also emits kind:'hybrid' edges from a synthetic question anchor to each retrieved chunk, grounded to real chunk_ids, no scores (D-05 option a)"
    - "bridgeResolve returns the real same_as edge (hub↔leaf) it traversed as kind:'traversed', label:'same_as' (SC-2)"
    - "every kind:'traversed' edge a tool emits is in the AQL-returned edge-id set (D-04 integration assertion)"
    - "the tools' existing data[] row shape the planner reads is unchanged (edges ride on a new field — additive, no eval impact)"
  artifacts:
    - path: "agent/src/tools/hybridRetrieve.ts"
      provides: "PART_OF traversed edges + question→chunk hybrid edges on the fragment"
      contains: "edge"
    - path: "agent/src/tools/bridgeResolve.ts"
      provides: "same_as traversed edges on the fragment"
      contains: "same_as"
    - path: "agent/test/hybridRetrieve.test.ts"
      provides: "live-guarded assertion that traversed edge _ids ⊆ AQL-returned set + hybrid edge presence"
    - path: "agent/test/bridgeResolve.test.ts"
      provides: "live-guarded assertion that same_as traversed edge _ids ⊆ AQL-returned set"
  key_links:
    - from: "agent/src/tools/hybridRetrieve.ts"
      to: "agent/src/envelope.ts"
      via: "builds RetrievalPathEdge objects for edges[]"
      pattern: "kind: 'traversed'"
    - from: "agent/src/tools/bridgeResolve.ts"
      to: "agent/src/envelope.ts"
      via: "builds RetrievalPathEdge objects for edges[]"
      pattern: "label: 'same_as'"
---

<objective>
Enrich the two REAL-traversal tools to capture and emit the edges they actually walk. hybridRetrieve traverses Chunk-PART_OF→Document (SC-1) and additionally fans out a synthetic question→chunk hybrid representation (D-05 option a, decided in RESEARCH). bridgeResolve traverses hub-same_as→leaf (SC-2).

Purpose: VIZ-01 — the retrievalPath must carry the actual traversed edges so Phase 11 renders what happened. The honesty contract (D-04) requires every kind:'traversed' edge to correspond to an edge the AQL actually returned; the integration tests prove this against the live cluster.

Output: PART_OF + hybrid edges on hybridRetrieve's fragment; same_as edges on bridgeResolve's fragment; extended live-guarded tests asserting the traversed/AQL-returned consistency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-answer-provenance-edge-enrichment/10-CONTEXT.md
@.planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md
@.planning/phases/10-answer-provenance-edge-enrichment/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hybridRetrieve — capture the PART_OF traversed edge + synthesize hybrid question→chunk edges</name>
  <files>agent/src/tools/hybridRetrieve.ts, agent/test/hybridRetrieve.test.ts</files>
  <read_first>
    - agent/src/tools/hybridRetrieve.ts (the file being modified — the OUTBOUND PART_OF AQL lines 110-123; fragment build lines 126-131; module constants RELATIONS/DOCUMENTS/CHUNKS lines 32-35)
    - agent/src/envelope.ts (RetrievalPathEdge + edges[] from 10-01; EdgeKindEnum)
    - .planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md (§"SC-1: hybridRetrieve" exact RETURN diff — add `, edge` second loop var and `edge:{_id,_from,_to}` to the row; §"D-05 Decision" exact hybrid shape: synthetic anchor `question/current`, one kind:'hybrid' edge per chunk _from anchor _to chunk_id, _id `hybrid:<anchor>:<chunk_id>`, NO scores; §"Pitfall 2" capture e._from/_to verbatim; §"Pitfall 4" edges ride inside the fragment so streaming gets them free)
    - agent/test/hybridRetrieve.test.ts (extend; reuse the existing canRun = hasLiveDb() && hasOpenAi() guard and describe.skipIf pattern)
  </read_first>
  <behavior>
    - (live-guarded) runHybridRetrieve emits a fragment whose edges[] contains ≥1 edge with kind:'traversed', label:'PART_OF', collection: RELATIONS, and _from/_to taken verbatim from the AQL edge document
    - (live-guarded) every kind:'traversed' edge _id is present in the set of edge._id values the AQL returned on the data rows (traversedEdgesAreGrounded true — D-04)
    - (live-guarded) edges[] contains kind:'hybrid' edges (label:'hybrid'), one per retrieved chunk, each _to a chunk_id that is also in retrievalPath._ids, with NO vector/BM25/RRF score field
    - the HybridChunk data shape the planner reads is unchanged except for the new optional edge field on the row (chunk_id/content/account_id/citable_url/file_name preserved)
  </behavior>
  <action>
    In runHybridRetrieve, change the sourcing traversal from `FOR doc IN 1..1 OUTBOUND chunkId ${literal(RELATIONS)}` to `FOR doc, edge IN 1..1 OUTBOUND chunkId ${literal(RELATIONS)}` and add `edge: { _id: edge._id, _from: edge._from, _to: edge._to }` as a NEW field on the RETURN object (keep all existing row fields). Add edge to the HybridChunk interface as an optional field. Build the fragment edges[] = for each data row, a RetrievalPathEdge { _id: row.edge._id, _from: row.edge._from, _to: row.edge._to, collection: RELATIONS, kind: 'traversed', label: 'PART_OF' }; capture _from/_to verbatim from the edge doc (do not reconstruct — Pitfall 2). Then ADD the D-05 hybrid edges: a deterministic synthetic anchor constant (e.g. 'question/current'), and for each chunk_id in topK/data, a RetrievalPathEdge { _id: `hybrid:question/current:${chunk_id}`, _from: 'question/current', _to: chunk_id, collection: 'hybrid', kind: 'hybrid', label: 'hybrid' } — NO score fields (D-03). Set retrievalPath.edges to the concatenation [...partOfEdges, ...hybridEdges]. Do not touch _ids/graph/collection/query. In agent/test/hybridRetrieve.test.ts add, under the existing describe.skipIf(!canRun), assertions for the four behaviors above; derive the AQL-returned ground-truth edge-id set from data.map(d => d.edge._id) and use traversedEdgesAreGrounded (imported from src/retrievalPath.js).
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/hybridRetrieve.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - hybridRetrieve.ts AQL uses `FOR doc, edge IN 1..1 OUTBOUND` and the RETURN row carries `edge: { _id, _from, _to }`
    - hybridRetrieve.ts fragment edges[] contains an edge with kind:'traversed', label:'PART_OF', collection === RELATIONS
    - hybridRetrieve.ts emits kind:'hybrid' edges with _from 'question/current', _to a real chunk_id, and NO score field
    - the test derives ground truth from data.map(d => d.edge._id) and asserts traversedEdgesAreGrounded(fragment, groundTruth) === true
    - cd agent && npx vitest run test/hybridRetrieve.test.ts exits 0 (live tests skip cleanly when canRun false; pure tsc still proves shape)
    - cd agent && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>hybridRetrieve captures the PART_OF edge as kind:'traversed' and emits D-05 hybrid edges; the live-guarded test proves traversed ⊆ AQL-returned; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bridgeResolve — capture the same_as traversed edge (both subqueries)</name>
  <files>agent/src/tools/bridgeResolve.ts, agent/test/bridgeResolve.test.ts</files>
  <read_first>
    - agent/src/tools/bridgeResolve.ts (the file being modified — the two INBOUND same_as subqueries lines 55-64; the structured subquery binds vertex `leaf`, the kg subquery binds vertex `e` at line 61; fragment build lines 76-83; SPLIT(_id,"/")[0] idiom)
    - agent/src/envelope.ts (RetrievalPathEdge from 10-01)
    - .planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md (§"SC-2: bridgeResolve" exact diff — add edge var to the structured subquery; AVOID name collision with kg's existing vertex `e` by renaming or choosing distinct edge var names; §"Pitfall 2" INBOUND direction — _from is the leaf, _to is the hub, capture verbatim; §"Open Questions Q1" capture the KG-side same_as edge too as kind:'traversed' label:'same_as' — same collection, free, Phase 11 can hide)
    - agent/test/bridgeResolve.test.ts (extend; reuse the existing canRun live guard and a known demo-critical entityId fixture)
  </read_first>
  <behavior>
    - (live-guarded) resolve(entityId) for a demo-critical id emits a fragment whose edges[] contains ≥1 kind:'traversed', label:'same_as', collection:'same_as' edge
    - (live-guarded) edge _from/_to are captured verbatim from the AQL edge doc (INBOUND: _from is the leaf, _to is the hub) — not reconstructed
    - (live-guarded) every kind:'traversed' edge _id is in the AQL-returned same_as edge-id set (traversedEdgesAreGrounded true — D-04)
    - the BridgeHub data shape (canonical_id/display_name/entity_type/account_id/structured_nodes/kg_nodes) is unchanged except for a new edge field carried alongside each node
  </behavior>
  <action>
    In resolve(), add the edge loop variable to BOTH same_as subqueries. In the `structured` subquery change `FOR leaf IN 1..1 INBOUND hub._id same_as` to `FOR leaf, edge IN ...` and add `edge: { _id: edge._id, _from: edge._from, _to: edge._to }` to that RETURN object. In the `kg` subquery the vertex is currently bound as `e` (line 61) — rename it to avoid a collision (e.g. vertex `kgNode`, edge `kgEdge`: `FOR kgNode, kgEdge IN 1..1 INBOUND hub._id same_as`) and add the same edge field to its RETURN. Update the BridgeNode interface to carry an optional edge. Build the fragment edges[] from every structured-node and kg-node edge: RetrievalPathEdge { _id: edge._id, _from: edge._from, _to: edge._to, collection: 'same_as', kind: 'traversed', label: 'same_as' } — capture _from/_to verbatim (Pitfall 2, INBOUND). Dedup is handled downstream by mergeRetrievalPaths; the tool may emit duplicates. Do not change _ids/graph/collection/query on the fragment. In agent/test/bridgeResolve.test.ts add, under the existing canRun guard, the four assertions above; build the ground-truth edge-id set from the AQL-returned edge._id values and assert traversedEdgesAreGrounded(fragment, groundTruth) === true.
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/bridgeResolve.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - bridgeResolve.ts both same_as subqueries bind an edge variable and RETURN `edge: { _id, _from, _to }` (kg subquery vertex renamed off `e` to avoid collision)
    - bridgeResolve.ts fragment edges[] contains an edge with kind:'traversed', label:'same_as', collection:'same_as'
    - edge _from/_to are taken straight from the edge doc (INBOUND: _from === leaf, _to === hub)
    - the test asserts traversedEdgesAreGrounded(fragment, AQL-returned same_as edge-id set) === true
    - cd agent && npx vitest run test/bridgeResolve.test.ts exits 0
    - cd agent && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>bridgeResolve captures the same_as edge from both subqueries as kind:'traversed'; the live-guarded test proves traversed ⊆ AQL-returned; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool AQL → fragment edges[] | the honesty boundary: a "traversed" edge must correspond to a real walked edge |
| query text → AQL | unchanged — no new bind values; only the `, edge`/`, kgEdge` loop variable and static literals are added |

This plan is additive read-side enrichment of two AQL RETURN clauses. RESEARCH §Security Domain confirms: no new bind values, no interpolated names → AQL injection surface is UNCHANGED. No new external input, no auth/PII surface. The two real threats are the honesty/fabrication threat and the grounding-leak threat; padding with generic web-app threats would be dishonest.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering/Repudiation (fabricated traversal) | hybridRetrieve PART_OF edge + bridgeResolve same_as edge | mitigate | Each tool's edges[] is built ONLY from edge._id/_from/_to values the AQL actually returned; the live-guarded integration assertion uses traversedEdgesAreGrounded against the AQL-returned set so a fabricated kind:'traversed' edge fails the test (D-04). The D-05 hybrid edges are kind:'hybrid' (never claimed as traversals) and exempt. |
| T-10-04 | Tampering (AQL injection) | enriched RETURN clauses | accept | RELATIONS/DOCUMENTS/'same_as' remain literal()/string constants; only the second loop variable is added; no new bind value. Injection surface unchanged per RESEARCH §Security Domain. |
| T-10-05 | Information Disclosure (grounding leak) | edges[] vs returnedIds | mitigate | Edges ride on the fragment's new edges[] field; _ids is unchanged, so the agent.ts/stream.ts returnedIds loop (built only from frag._ids) never sees an edge _id. The returnedIds loop itself is verified untouched in 10-03. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: Package Legitimacy Audit N/A). |
</threat_model>

<verification>
- `cd agent && npx vitest run test/hybridRetrieve.test.ts test/bridgeResolve.test.ts` exits 0 (live edge assertions run when ARANGO_*/OPENAI_API_KEY present; skip cleanly otherwise)
- `cd agent && npx tsc --noEmit` exits 0 (SC-3)
- (manual, per VALIDATION.md) the data[] row shape the planner reads is unchanged — eval gate additivity proven in 10-03
</verification>

<success_criteria>
- hybridRetrieve emits PART_OF kind:'traversed' edges (SC-1) AND D-05 hybrid edges (no scores)
- bridgeResolve emits same_as kind:'traversed' edges from both subqueries (SC-2)
- Every traversed edge is in the AQL-returned set (D-04 integration)
- No change to the planner-read data row fields → no eval-verdict impact
- tsc clean
</success_criteria>

<output>
Create `.planning/phases/10-answer-provenance-edge-enrichment/10-02-SUMMARY.md` when done.
</output>
