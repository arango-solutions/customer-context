---
phase: 10-answer-provenance-edge-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/envelope.ts
  - agent/src/retrievalPath.ts
  - agent/test/envelope.test.ts
  - agent/test/retrievalPath.test.ts
autonomous: true
requirements: [VIZ-01]

must_haves:
  truths:
    - "RetrievalPathFragment carries an edges[] array that defaults to [] when a tool emits no edges (entityLookup parses cleanly)"
    - "An edge object discriminates 'traversed' | 'structural' | 'hybrid' via a kind enum and carries a human-readable label, orthogonal to graph"
    - "mergeRetrievalPaths unions edges within a (graph,collection,query) group and drops no sourced edge"
    - "A pure no-fabrication guard fails when a kind:'traversed' edge is not in the AQL-returned edge-id set (D-04)"
    - "tsc is clean after the schema change (SC-3)"
  artifacts:
    - path: "agent/src/envelope.ts"
      provides: "RetrievalPathEdge Zod object + EdgeKindEnum + edges[] on RetrievalPathFragment"
      contains: "RetrievalPathEdge"
    - path: "agent/src/retrievalPath.ts"
      provides: "mergeRetrievalPaths edge union + dedup"
      contains: "edges"
    - path: "agent/test/retrievalPath.test.ts"
      provides: "edge-merge + D-04 no-fabrication guard + returnedIds non-leak pure tests"
    - path: "agent/test/envelope.test.ts"
      provides: "RetrievalPathEdge parse + edges default-[] assertions"
  key_links:
    - from: "agent/src/retrievalPath.ts"
      to: "agent/src/envelope.ts"
      via: "imports RetrievalPathFragmentT (now with edges)"
      pattern: "RetrievalPathFragmentT"
---

<objective>
Establish the edge data-model contract and the merge + honesty-guard foundation that the tool-enrichment plans (10-02, 10-03) build against. This is the Wave 0 test scaffold (per VALIDATION.md) plus the shared Zod contract — the single source of truth every tool and both agent loops import.

Purpose: VIZ-01 requires RetrievalPathFragment to gain edges[]; Phase 11 (VIZ-02) is the sole consumer and needs the three-kind discriminator (D-01) + label (D-03) to style edges with no re-query. The no-fabrication guard (D-04) is the load-bearing honesty control — a confident-wrong traversal is the failure mode the project exists to avoid.

Output: RetrievalPathEdge + EdgeKindEnum + edges[] in envelope.ts; edge union/dedup in mergeRetrievalPaths; pure tests in retrievalPath.test.ts and envelope.test.ts that the downstream plans satisfy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-answer-provenance-edge-enrichment/10-CONTEXT.md
@.planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md
@.planning/phases/10-answer-provenance-edge-enrichment/10-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RetrievalPathEdge + edges[] to the shared envelope contract</name>
  <files>agent/src/envelope.ts, agent/test/envelope.test.ts</files>
  <read_first>
    - agent/src/envelope.ts (the file being modified — RetrievalPathFragment is lines 48-54; GraphEnum line 20; do not fork the contract per the file header)
    - agent/test/envelope.test.ts (extend; existing validEnvelope fixture + Zod parse/reject patterns)
    - .planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md (§"envelope.ts — the new RetrievalPathEdge + edges[] field" gives the exact Zod shape; §"Pitfall 5 Zod 4 default-array" — use .default([]) not .optional(); the codebase uses the .nullable() METHOD form)
  </read_first>
  <behavior>
    - RetrievalPathEdge with a real edge _id parses successfully
    - RetrievalPathEdge with _id: null parses successfully (synthetic/structural/hybrid edges may have null)
    - A RetrievalPathFragment object omitting edges parses and yields edges === []
    - kind outside ['traversed','structural','hybrid'] is rejected
  </behavior>
  <action>
    In agent/src/envelope.ts add, near RetrievalPathFragment: export const EdgeKindEnum = z.enum(['traversed','structural','hybrid']); export type EdgeKind; export const RetrievalPathEdge = z.object with fields _id (z.string().nullable() — method form, matching the SynthRetrievalPath._ids idiom), _from (z.string()), _to (z.string()), collection (z.string()), kind (EdgeKindEnum), label (z.string()); export type RetrievalPathEdgeT. Add edges: z.array(RetrievalPathEdge).default([]) as a new field on RetrievalPathFragment. Do NOT change graph, collection, _ids, or query. Add a comment that label is one of 'PART_OF' | 'same_as' | 'account' | 'hybrid' but kept z.string() for forward-compat. In agent/test/envelope.test.ts add a describe block asserting the four behaviors above using EdgeKindEnum.options or RetrievalPathEdge.parse / RetrievalPathFragment.parse and .safeParse for the reject case; assert edges defaults to [] by parsing a fragment literal with no edges key.
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/envelope.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - envelope.ts exports RetrievalPathEdge with kind: z.enum(['traversed','structural','hybrid'])
    - envelope.ts RetrievalPathFragment has edges: z.array(RetrievalPathEdge).default([])
    - RetrievalPathEdge._id accepts both a string and null (parsed without throwing)
    - cd agent && npx vitest run test/envelope.test.ts exits 0
    - cd agent && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>RetrievalPathEdge + EdgeKindEnum + edges[] live in envelope.ts; envelope.test.ts proves parse/default/reject; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Union edges in mergeRetrievalPaths + add the D-04 guard and returnedIds non-leak tests (Wave 0)</name>
  <files>agent/src/retrievalPath.ts, agent/test/retrievalPath.test.ts</files>
  <read_first>
    - agent/src/retrievalPath.ts (the file being modified — mergeRetrievalPaths groups by graph::collection::query, first-seen dedup of _ids lines 38-59; cleanIds null-strip chokepoint)
    - agent/src/envelope.ts (RetrievalPathEdge / RetrievalPathFragmentT shape from Task 1)
    - .planning/phases/10-answer-provenance-edge-enrichment/10-RESEARCH.md (§"Pattern 3: Edge merge" gives the edgeKey fn: e._id ?? `${e.kind}::${e._from}::${e._to}::${e.label}`; §"Honesty Contract & D-04 Guard Test" gives the traversedEdgesAreGrounded guard signature and the three required test types)
    - agent/test/envelope.test.ts (test idiom reference)
  </read_first>
  <behavior>
    - Two fragments in the same (graph,collection,query) group with distinct edges → merged group contains the union of both edge sets, none dropped
    - Two fragments carrying the SAME edge _id → merged group contains that edge once (deduped)
    - An edge with _id == null dedups on the composite `${kind}::${_from}::${_to}::${label}` key (no crash, no over-merge)
    - traversedEdgesAreGrounded(frag, returnedEdgeIds) returns true when every kind:'traversed' edge _id is in the set; returns false when a traversed edge _id is absent (D-04)
    - structural and hybrid edges are EXEMPT from the guard (a structural edge with an id not in the traversed set does NOT fail)
    - No edge _id (any kind) appears in a returnedIds set built only from frag._ids (non-leak assertion mirroring agent.ts/stream.ts)
  </behavior>
  <action>
    In agent/src/retrievalPath.ts extend mergeRetrievalPaths to carry edges: when creating a new group, clone edges (do not mutate caller fragments) into the group entry as edges: []; populate via the same first-seen-wins Set pattern proven for _ids, keyed by edgeKey = (e) => e._id ?? `${e.kind}::${e._from}::${e._to}::${e.label}`. When merging into an existing group, union new edges by the same key. Ensure a fragment with no edges (undefined or []) contributes nothing and does not crash. Export the pure guard helper traversedEdgesAreGrounded(frag, returnedEdgeIds: Set<string>): boolean that filters edges to kind==='traversed' and requires every such edge to have a non-null _id present in returnedEdgeIds (structural/hybrid exempt) — place it in retrievalPath.ts so both the tests and tool tests import one implementation. In agent/test/retrievalPath.test.ts (NEW file) write pure tests for all six behaviors above; for the non-leak assertion, simulate the agent.ts/stream.ts loop by building returnedIds only from frag._ids and asserting no edge._id is a member.
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/retrievalPath.test.ts test/envelope.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - retrievalPath.ts mergeRetrievalPaths unions edges with dedup key e._id ?? `${e.kind}::${e._from}::${e._to}::${e.label}`
    - retrievalPath.ts exports traversedEdgesAreGrounded(frag, returnedEdgeIds)
    - traversedEdgesAreGrounded returns false for a kind:'traversed' edge whose _id is absent from the set, true when all present, and is unaffected by kind:'structural'/'hybrid' edges
    - retrievalPath.test.ts asserts no edge _id enters a returnedIds set built only from frag._ids
    - cd agent && npx vitest run test/retrievalPath.test.ts test/envelope.test.ts exits 0
    - cd agent && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>mergeRetrievalPaths unions/dedups edges without dropping any; traversedEdgesAreGrounded exported; retrievalPath.test.ts covers merge + D-04 + non-leak; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool AQL output → fragment edges[] | the only place a "traversed" claim is made; honesty boundary lives here |
| fragment edges[] → grounding returnedIds | additivity boundary — edges must NOT cross into grounding anchors |

This plan adds NO external input, NO bind values, NO auth/PII surface. It is a pure-TS contract + merge + test change. The threat surface is narrow and the two threats below are the only ones worth modeling; padding with web-app threats would be dishonest.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering/Repudiation | mergeRetrievalPaths + RetrievalPathEdge.kind | mitigate | traversedEdgesAreGrounded (D-04 guard) makes a structural/hybrid edge mislabeled `traversed` (a "confident-wrong-graph") a test failure; structural/hybrid edges are explicitly exempt because they are never claimed as traversals. Tested in retrievalPath.test.ts this plan. |
| T-10-02 | Information Disclosure (grounding leak) | edges[] vs returnedIds | mitigate | The non-leak test asserts no edge _id enters a returnedIds set built only from frag._ids — encoding the SC-5 isolation rule at the data layer. The actual agent.ts/stream.ts loops are verified untouched in 10-03. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH: Package Legitimacy Audit N/A — zod/arangojs/ai/vitest all already present). No install task exists. |
</threat_model>

<verification>
- `cd agent && npx vitest run test/envelope.test.ts test/retrievalPath.test.ts` exits 0 (pure, no DB/OpenAI creds needed)
- `cd agent && npx tsc --noEmit` exits 0 (SC-3)
- The D-04 guard test demonstrably FAILS when fed a fabricated traversed edge (negative case asserted in retrievalPath.test.ts)
</verification>

<success_criteria>
- RetrievalPathEdge (with EdgeKindEnum + label) and edges[] (.default([])) exist in envelope.ts (SC-3)
- mergeRetrievalPaths unions and dedups edges, dropping none (VIZ-01 merge)
- traversedEdgesAreGrounded encodes the D-04 honesty invariant and the negative case fails
- The returnedIds non-leak assertion passes (SC-5 isolation, data-layer)
- tsc clean
</success_criteria>

<output>
Create `.planning/phases/10-answer-provenance-edge-enrichment/10-01-SUMMARY.md` when done.
</output>
