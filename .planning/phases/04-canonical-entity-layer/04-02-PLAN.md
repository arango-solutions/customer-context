---
phase: 04-canonical-entity-layer
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - scripts/verify_entity_bridge.py
  - scripts/verify_coref_eval.py
autonomous: true
requirements:
  - ENT-01
  - ENT-02

must_haves:
  truths:
    - "`python scripts/verify_entity_bridge.py --full` exits 0 — D-04 no-double-resolution check passes AND bijection check passes AND D-05 100% demo-critical gate passes AND ENT-02 trace probe returns a renderable cross-graph fragment"
    - "`python scripts/verify_coref_eval.py --quick` exits 0 with coref accuracy at 100% for demo-critical entities after Phase 4 stamps entity_id"
    - "The ENT-02 trace probe emits a line containing 'kg_entity_name', 'canonical_id', 'match_method', and 'structured_nodes' — a renderable retrieval-path fragment"
    - "D-04 Check 1 (no double-resolution) returns 0 rows from the ArangoDB AQL — no entity_id appears on more than one customer360_Entities document"
    - "D-04 Check 2 (bijection) returns 0 violations — every demo-critical entity present in the KG has exactly one same_as edge directed leaf → hub"
    - "`python scripts/verify_entity_bridge.py --quick` exits 0 in < 30 seconds (per-plan quick gate)"
  artifacts:
    - path: "scripts/verify_entity_bridge.py"
      provides: "D-04 integrity gate + D-05 demo-critical 100% gate + ENT-02 trace probe"
      min_lines: 200
    - path: "scripts/verify_coref_eval.py"
      provides: "Extended to default threshold=100% + demo-critical subset hard gate"
  key_links:
    - from: "scripts/verify_entity_bridge.py"
      to: "ArangoDB:customer360_Entities"
      via: "AQL FILTER e.entity_id != null ... COLLECT ... FILTER cnt > 1"
      pattern: "customer360_Entities.*entity_id"
    - from: "scripts/verify_entity_bridge.py"
      to: "ArangoDB:same_as"
      via: "AQL OUTBOUND same_as traversal for ENT-02 trace probe"
      pattern: "OUTBOUND.*same_as"
    - from: "scripts/verify_entity_bridge.py"
      to: "ArangoDB:canonical_entities"
      via: "AQL 1..1 OUTBOUND traversal to hub"
      pattern: "canonical_entities"
---

<objective>
Build `scripts/verify_entity_bridge.py` (new) and extend `scripts/verify_coref_eval.py` to implement the D-04 integrity gate, D-05 demo-critical correctness gate, and ENT-02 cross-graph trace probe.

Purpose: Phase 4's validation layer. The bridge built in plan 04-01 must be asserted correct before Phase 5 consumes it. A confident wrong cross-graph link in a live Zscaler demo is the defined failure mode to avoid. This plan implements the hard gates that block on any correctness failure.

Output:
- `scripts/verify_entity_bridge.py` — new script with D-04 integrity gate (no-double-resolution + bijection), D-05 demo-critical 100% gate, ENT-02 trace probe, and `--quick` / `--full` CLI
- `scripts/verify_coref_eval.py` — extended: default threshold raised to 100%, demo-critical subset hard-gate block added to main()
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-canonical-entity-layer/04-CONTEXT.md
@.planning/phases/04-canonical-entity-layer/04-RESEARCH.md
@.planning/phases/04-canonical-entity-layer/04-PATTERNS.md
@.planning/phases/04-canonical-entity-layer/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build verify_entity_bridge.py (D-04 gate + ENT-02 trace probe)</name>

  <read_first>
    - `scripts/verify_graphs.py` — MANDATORY analog: module header + imports (lines 1-49), `_get_db()` (lines 111-131), `has_collection()` guard pattern (lines 151-153, 215-218, 235-238), check function signature `-> tuple[bool, str]` (line 136), gate runner + argparse main() structure
    - `scripts/verify_coref_eval.py` — `_get_db()` variant (lines 62-100); argparse pattern (lines 239-266); SKIP message pattern for missing collection
    - `.planning/phases/04-canonical-entity-layer/04-PATTERNS.md` — FULL FILE: module header template for verify_entity_bridge.py, `_DEMO_CRITICAL_ENTITIES` constant (the full dict with all 9 entity_ids + display names), `check_no_double_resolution()`, `check_bijection()`, `check_demo_critical()`, `probe_trace()`, gate runner `main()` with `--quick` / `--full` / `--check` / `--probe` argparse
    - `.planning/phases/04-canonical-entity-layer/04-RESEARCH.md` — §4 (D-04 AQL patterns, check 1 and check 2, expected 0-row result = PASS), §5 (demo-critical entity set verified UUIDs, D-05 implementation), §6 (ENT-02 trace AQL producing renderable fragment), §7 (rebuild-chain position: this script runs after build_entity_bridge.py)
    - `scripts/build_entity_bridge.py` — confirm actual collection names used (must match `_COLLECTION_ENTITIES`, `_COLLECTION_CANONICAL`, `_COLLECTION_SAME_AS` constants used during the build)
  </read_first>

  <files>scripts/verify_entity_bridge.py</files>

  <action>
    Create `scripts/verify_entity_bridge.py` as a new file. Mirror the structure of `scripts/verify_graphs.py` exactly (module docstring, imports, _REPO_ROOT/sys.path, load_dotenv, constants, _get_db(), check functions, probe functions, main()).

    Module docstring: Document all checks (bridge-collections, no-double-resolution, bijection, entity-id-stamp, demo-critical, trace) and usage (`--quick`, `--full`, `--check NAME`, `--probe NAME`). Include AQL safety statement.

    Constants:
    - `_COLLECTION_ENTITIES = "customer360_Entities"`, `_COLLECTION_CANONICAL = "canonical_entities"`, `_COLLECTION_SAME_AS = "same_as"`
    - `_DEMO_CRITICAL_ENTITIES: dict[str, dict]` — the full 9-entity constant from PATTERNS.md with all verified UUIDs (James Okafor, Taylor Brooks, Patricia Vance, Sarah Chen, Michael Torres, Meridian Logistics, Northwind Analytics, Enterprise 2026, ArangoGraph 2026). These UUIDs are cross-referenced in RESEARCH.md §5 and PATTERNS.md.
    - `_DEMO_CRITICAL_IDS = list(_DEMO_CRITICAL_ENTITIES.keys())`

    `_get_db()`: Copy verbatim from `verify_coref_eval.py` lines 62-100 (it already has the ARANGO_URL/ARANGO_ENDPOINT dual-fallback pattern). `user_token=jwt` not `auth_token=jwt`.

    `check_bridge_collections(db) -> tuple[bool, str]`: Verify `canonical_entities` and `same_as` both exist (`db.has_collection(...)`) and are non-empty (`db.collection(...).count() > 0`). Return FAIL if either is missing or empty.

    `check_entity_id_stamp(db) -> tuple[bool, str]`: Query `FOR e IN customer360_Entities FILTER e.entity_id IN @demo_critical_ids RETURN DISTINCT e.entity_id` with `bind_vars={"demo_critical_ids": _DEMO_CRITICAL_IDS}`. Count found. Any demo-critical entity_id missing → include in failure message. Return PASS if all 9 are stamped, FAIL with specific missing names otherwise. Use `db.has_collection(_COLLECTION_ENTITIES)` guard first (return SKIP if missing).

    `check_no_double_resolution(db) -> tuple[bool, str]`: AQL from RESEARCH.md §4 Check 1. `FOR e IN customer360_Entities FILTER e.entity_id != null COLLECT eid = e.entity_id WITH COUNT INTO cnt FILTER cnt > 1 RETURN {entity_id: eid, count: cnt}` — NO bind_vars (no user input; collection name hardcoded). Expected 0 rows = PASS. Use `db.has_collection` guard.

    `check_bijection(db) -> tuple[bool, str]`: AQL from RESEARCH.md §4 Check 2 and PATTERNS.md. `FOR e IN customer360_Entities FILTER e.entity_id IN @demo_critical_ids LET edge_count = LENGTH(FOR ed IN same_as FILTER ed._from == e._id RETURN 1) FILTER edge_count != 1 RETURN {entity_name: e.entity_name, entity_id: e.entity_id, edge_count: edge_count}` with `bind_vars={"demo_critical_ids": _DEMO_CRITICAL_IDS}`. Expected 0 rows = PASS. Handle graceful SKIP if `same_as` collection does not exist.

    `check_demo_critical(db) -> tuple[bool, str]`: AQL from PATTERNS.md `check_demo_critical()`. Query for DISTINCT entity_ids in `customer360_Entities` matching `_DEMO_CRITICAL_IDS`. Any missing → named in the FAIL message (use `_DEMO_CRITICAL_ENTITIES[eid]['display_name']` for readable output). Must print ratio `N/9 = X%` in message. D-05 HARD GATE — caller treats False return as `sys.exit(1)`.

    `probe_trace(db) -> tuple[bool, str]`: ENT-02 trace AQL from RESEARCH.md §6 and PATTERNS.md. Step 1: find a sample demo-critical KG entity key (`FOR e IN customer360_Entities FILTER e.entity_id IN @demo_critical_ids LIMIT 1 RETURN e._key`). Step 2: run the full traversal AQL starting from that key — traverse `1..1 OUTBOUND e._id same_as` to reach the hub, then `1..1 INBOUND hub._id same_as` to find structured leaf nodes. Return a result dict containing `kg_entity_name`, `canonical_id`, `display_name`, `entity_type`, `match_method`, `matched_surface_form`, `confidence`, `structured_nodes`. Validate the result has `canonical_id` and `structured_nodes` is non-empty. Return PASS with a readable trace line like `[bridge-verify] Probe trace (ENT-02): 'James Okafor' → hub/633f43bd via deterministic → 1 structured node(s)`.

    `main()`: Argparse with mutually exclusive `--quick` / `--full`, plus `--check NAME` and `--probe NAME` for individual gates. Gate runner pattern from PATTERNS.md: collect `(name, fn)` pairs, call each, collect results, print `[bridge-verify] PASS: name — msg` or `[bridge-verify] FAIL: name — msg`. `sys.exit(1)` if any failed. Quick mode: `check_bridge_collections` + `check_entity_id_stamp`. Full mode: all checks + all probes.
  </action>

  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && python scripts/verify_entity_bridge.py --full 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `python scripts/verify_entity_bridge.py --quick` exits 0 in < 30 seconds
    - `python scripts/verify_entity_bridge.py --full` exits 0 with all checks PASS (assuming build_entity_bridge.py ran successfully in plan 04-01)
    - Output contains lines: `[bridge-verify] PASS: no-double-resolution`, `[bridge-verify] PASS: bijection`, `[bridge-verify] PASS: demo-critical`, `[bridge-verify] PASS: trace`
    - The trace probe line includes `'kg_entity_name'` text and `structured_nodes` count > 0
    - `python scripts/verify_entity_bridge.py --check no-double-resolution` exits 0 (individual check works)
    - `python scripts/verify_entity_bridge.py --probe trace` exits 0 and prints the renderable trace fragment (ENT-02)
    - No AQL f-string construction: `grep -n 'f".*FOR\|f".*FILTER\|f".*RETURN' scripts/verify_entity_bridge.py` returns empty
  </acceptance_criteria>

  <done>
    `verify_entity_bridge.py` exists and is fully implemented. `--full` run exits 0 with PASS on all 5 checks + trace probe. D-04 (no-double-resolution + bijection) and D-05 (100% demo-critical) gates are functional and would exit 1 on failure.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend verify_coref_eval.py — raise threshold to 100% + demo-critical hard gate (D-05 per D-05 decision)</name>

  <read_first>
    - `scripts/verify_coref_eval.py` — MANDATORY: read the FULL file before any edit. Key sections: `_DEFAULT_THRESHOLD_PCT = 60` (line 55, to change to 100), `main()` function (lines 239-335, the insertion point for the demo-critical block), `measure_coref_accuracy()` return shape (lines 172-232, the `result` dict with `details` list, `total`, `correct`, `accuracy`)
    - `.planning/phases/04-canonical-entity-layer/04-PATTERNS.md` — "Change 1 — threshold default" and "Change 2 — demo-critical subset block" sections; the exact `DEMO_CRITICAL_IDS` set; the `demo_critical_correct`/`demo_critical_total` calculation; the `sys.exit(1)` condition (`dc_pct < 100`)
    - `.planning/phases/04-canonical-entity-layer/04-CONTEXT.md` — D-05 decision: 100% on demo-critical entities BLOCKS; report-only on the rest
  </read_first>

  <files>scripts/verify_coref_eval.py</files>

  <action>
    Edit `scripts/verify_coref_eval.py` with two targeted changes. Do NOT alter any other logic, function signatures, or behavior.

    Change 1 — default threshold: Change line `_DEFAULT_THRESHOLD_PCT = 60` to `_DEFAULT_THRESHOLD_PCT = 100`. Update the docstring phrase "Phase 3 threshold: 60% accuracy" to "Phase 4 threshold: 100% required for demo-critical entities; overall coref accuracy must reach 100% after entity_id stamp." Keep the `--threshold N` argparse override so the value can still be overridden for diagnostic runs (per D-05, the default is the gate value, not a hard-coded check).

    Change 2 — demo-critical subset block: In `main()`, immediately after the existing `[coref] Result` print and before `sys.exit(0 if passed else 1)`, insert the demo-critical subset calculation and gate from PATTERNS.md. The block:
    - Defines `DEMO_CRITICAL_IDS` as a set of 9 UUIDs (copy from PATTERNS.md "Change 2" section; include all 9 entity_ids for James Okafor, Taylor Brooks, Patricia Vance, Sarah Chen, Michael Torres, Meridian Logistics, Northwind Analytics, Enterprise 2026 contract, ArangoGraph 2026 contract)
    - Calculates `demo_critical_correct` = count of `result["details"]` entries where `d["expected_entity_id"] in DEMO_CRITICAL_IDS and d["match"]`
    - Calculates `demo_critical_total` = count of distinct expected_entity_ids in details that are in DEMO_CRITICAL_IDS
    - If `demo_critical_total > 0`: compute `dc_pct`, print `[coref] Demo-critical accuracy: {demo_critical_correct}/{demo_critical_total} = {dc_pct:.0f}% — {dc_label} (must be 100%)`. If `dc_pct < 100`: call `sys.exit(1)` (D-05 hard gate — blocks before the general `sys.exit(0 if passed else 1)` at end of main)
    - If `demo_critical_total == 0`: print `[coref] Demo-critical: no demo-critical mentions in ground_truth set (coref_hard docs may not cover all entity types)` and do not block

    No other changes to the file.
  </action>

  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && python scripts/verify_coref_eval.py --quick 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - `python scripts/verify_coref_eval.py --quick` exits 0 (after Phase 4 bridge build populated entity_id; coref accuracy at 100% on demo-critical set)
    - Output contains `[coref] Demo-critical accuracy:` line showing N/N = 100% PASS
    - Output contains `[coref] Coref accuracy:` line with threshold showing 100%
    - `python scripts/verify_coref_eval.py --quick --threshold 60` still works (override respected) — exits 0 (backward-compatible diagnostic mode)
    - `grep '_DEFAULT_THRESHOLD_PCT' scripts/verify_coref_eval.py` shows value 100 (not 60)
    - The file still exits 0 with SKIP message when coref_hard entries are absent in manifest.json (no regression to the Phase 3 graceful-skip behavior — test: `python -c "import json, pathlib; p=pathlib.Path('data_gen/output/manifest.json'); d=json.loads(p.read_text()); print(sum(1 for v in d.values() if isinstance(v,dict) and v.get('coref_hard')))"` — if count > 0 the skip path is not exercised in live run; the SKIP logic remains intact in the code)
  </acceptance_criteria>

  <done>
    `verify_coref_eval.py` updated: default threshold is 100%, demo-critical subset block hard-gates on < 100% for the 9 named entities the 6 questions traverse. General coref accuracy remains report-only beyond the demo-critical set. No existing behavior regressed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → verify scripts | DB credentials read at startup; OpenAI key not needed (verify is read-only) |
| verify scripts → ArangoDB live cluster | Read-only AQL queries over HTTPS; Bearer JWT |
| manifest.json → coref eval | Ground-truth mention map read from local file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02-01 | Tampering | AQL in all check/probe functions — entity_ids injected from `_DEMO_CRITICAL_IDS` constant | mitigate | All user-variable AQL values passed via `bind_vars` dict; `_DEMO_CRITICAL_IDS` is a module-level Python constant (not from user input, not from file read at runtime), making injection impossible; collection names are hardcoded string literals in AQL |
| T-4-02-02 | Information Disclosure | ARANGO_PASSWORD in verify scripts | mitigate | `load_dotenv(_REPO_ROOT / ".env", override=True)` at module top; credential not logged; `.env` gitignored |
| T-4-02-03 | Spoofing | D-05 gate bypassed by lowering threshold | accept | `_DEFAULT_THRESHOLD_PCT = 100` is code; `--threshold N` override documented for diagnostic use only; the demo-critical subset block in verify_coref_eval.py always gates at exactly 100% regardless of `--threshold` override (separate code path) |
| T-4-02-04 | Elevation of Privilege | verify script accidentally writes to DB | accept | All verify_entity_bridge.py AQL is read-only (`FOR ... FILTER ... RETURN`); no `INSERT`, `UPDATE`, `REMOVE`, or `UPSERT` in any check/probe function |
| T-4-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages — all dependencies already installed; no install tasks in this plan |
</threat_model>

<verification>
Full phase gate (both plans complete):

1. `python scripts/build_entity_bridge.py --alias-only` — exits 0, deterministic count > 0
2. `python scripts/build_entity_bridge.py` — exits 0, embedding step runs (or logs API error gracefully)
3. `python scripts/verify_entity_bridge.py --full` — exits 0, all checks PASS including D-05 100% demo-critical
4. `python scripts/verify_coref_eval.py --quick` — exits 0, `[coref] Demo-critical accuracy:` = 100% PASS
5. `python scripts/verify_entity_bridge.py --probe trace` — exits 0, trace fragment includes `canonical_id` and `structured_nodes` > 0 (ENT-02)
6. Idempotency: re-run `python scripts/build_entity_bridge.py --alias-only` a second time, then re-run `python scripts/verify_entity_bridge.py --full` — still exits 0 (no corruption from re-run)
</verification>

<success_criteria>
`verify_entity_bridge.py` exists with `--full` mode exiting 0: D-04 no-double-resolution check passes, D-04 bijection check passes, D-05 100% demo-critical gate passes, ENT-02 trace probe returns a renderable cross-graph fragment. `verify_coref_eval.py` defaults to 100% threshold and hard-gates on demo-critical failures. Every named entity the 6 locked questions traverse is correctly linked across both graphs.
</success_criteria>

<output>
Create `.planning/phases/04-canonical-entity-layer/04-02-SUMMARY.md` when done.
</output>
