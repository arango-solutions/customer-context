---
phase: 09-data-depth-3rd-account
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data_gen/spine/entity_registry.py
  - data_gen/spine/spine_helio.py
  - data_gen/generate.py
  - data_gen/generators/slack_generator.py
  - data_gen/generators/email_generator.py
  - data_gen/generators/docs_generator.py
  - data_gen/generators/pdf_generator.py
  - scripts/load_structured.py
  - data_gen/linter/conftest.py
  - data_gen/linter/test_answerability.py
  - data_gen/linter/test_near_miss_guard.py
  - agent/test/questions.eval.test.ts
  - agent/src/index.ts
  - scripts/eval-gate.ts
autonomous: true
requirements: [DATA-04]

must_haves:
  truths:
    - "A 3rd account 'Helio Retail' (Account C) exists with a churn/contraction arc (declining usage, downgrade contract, NPS score AND sentiment both falling) — distinct from Northwind growth and Meridian green-usage/red-sentiment framing (D-01, D-02, D-03)."
    - "C's 4 unstructured modules (helio_slack/email/docs/pdf) and 4 structured sources are declared in BOTH MODULE_NAMES copies and in load_structured SOURCE_FILES."
    - "C's signal docs carry contraction lexicon (downgrade / migration-away / declining-usage / deprioritization) — not Northwind nor Meridian prohibited vocabulary — via a helio branch in every generator _get_prohibited_terms AND a Q13/Q14 branch in every prose _derive_ helper."
    - "2-3 new C questions (Q13 flagship dual-graph, Q14 follow-up, Q15 structured-only anchor) are added to the locked eval set, conftest QUESTION_IDS, answerability tests, near-miss guard, and eval-gate LOCKED_QUESTION_LABELS (D-04)."
  artifacts:
    - path: "data_gen/spine/spine_helio.py"
      provides: "Account C contraction-arc spine (contracts/usage/nps/opportunities/contacts + signal/near-miss/noise docs)"
      contains: "HELIO_SPINE"
      min_lines: 200
    - path: "data_gen/spine/entity_registry.py"
      provides: "HELIO_ACCOUNT_ID + 4 helio_ modules in MODULE_NAMES (one-way door)"
      contains: "HELIO_ACCOUNT_ID"
    - path: "scripts/load_structured.py"
      provides: "Helio 6 structured input files registered in SOURCE_FILES"
      contains: "helio/crm/helio_crm_accounts.json"
    - path: "data_gen/linter/test_near_miss_guard.py"
      provides: "Q13 QUESTION_TEXTS entry + test_near_miss_guard_q13 scoped to helio modules"
      contains: "test_near_miss_guard_q13"
    - path: "agent/test/questions.eval.test.ts"
      provides: "C it blocks (Q13 dual-graph, Q14 follow-up, Q15 structured-only anchor)"
      contains: "Q13"
  key_links:
    - from: "data_gen/generate.py"
      to: "data_gen/spine/spine_helio.py"
      via: "import HELIO_SPINE; append to _SPINES"
      pattern: "HELIO_SPINE"
    - from: "data_gen/generators/_generator.py"
      to: "helio modules"
      via: "_get_prohibited_terms helio branch + Q13/Q14 prose branch"
      pattern: "helio"
    - from: "scripts/eval-gate.ts"
      to: "questions.eval.test.ts C blocks"
      via: "LOCKED_QUESTION_LABELS substring classify"
      pattern: "Q13"
---

<objective>
Build Account C ("Helio Retail" — e-commerce / retail personalization vertical, per D-02 Claude's discretion) end-to-end as a new deterministic spine, wire it into generation, fix the account-coupled generator/loader code paths so C's data is generated and loadable with its OWN lexicon, and add C's 2-3 new questions to the locked eval gate + near-miss guard. This satisfies DATA-04. Account C tells the "save" leg of the demo triad (A grows, B holds, C is slipping) via a usage-and-revenue CONTRACTION arc (D-01/D-03) — deliberately NOT Meridian's green-usage/red-sentiment contradiction.

Purpose: Adds the 3rd account + new question arcs the rest of v2.0 depends on, while keeping every fact deterministic and grounded so the linter and Phase 8 eval gate stay green.
Output: New spine_helio.py; widened coupling constants; helio-aware generators/loader; C's locked questions + empirical near-miss guard coverage. No regen/ingest here (that is Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-depth-3rd-account/09-CONTEXT.md
@.planning/phases/09-data-depth-3rd-account/09-RESEARCH.md
@.planning/phases/09-data-depth-3rd-account/09-PATTERNS.md
@.planning/phases/09-data-depth-3rd-account/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Account C spine + registry + generation wiring</name>
  <files>data_gen/spine/entity_registry.py, data_gen/spine/spine_helio.py, data_gen/generate.py</files>
  <read_first>
    - data_gen/spine/spine_meridian.py — the EXACT analog to mirror for spine_helio.py (AccountSpine construction, _usage/_nps helpers, _noise_doc, signal DocEvent shape).
    - data_gen/spine/spine_northwind.py — confirms both existing arcs RISE monotonically, so C's decline is unambiguous.
    - data_gen/spine/entity_registry.py — ACCOUNT_ID declaration pattern (lines 53-54) + MODULE_NAMES (lines 60-69, the one-way door); canonical_uuid / make_file_name / make_citable_url signatures (lines 34-46).
    - data_gen/spine/event_spine.py — AccountSpine, ArangoEdition, ContractEvent, UsageEvent, NpsEvent, OpportunityEvent, ContactEvent, DocEvent field definitions.
    - data_gen/generate.py — spine import block (lines 42-43), _SPINES (line 62), account_key derivation (lines 73-76, 110-117), unstructured generator calls (lines 123-133).
  </read_first>
  <action>
    In entity_registry.py: add HELIO_ACCOUNT_ID = canonical_uuid("helio", "helio_retail") after MERIDIAN_ACCOUNT_ID, and append module names helio_slack, helio_email, helio_docs, helio_pdf to MODULE_NAMES (the one-way door enforced by test_field_stamps.py::test_module_names_valid).
    Create data_gen/spine/spine_helio.py mirroring spine_meridian.py structure exactly, building HELIO_SPINE with account_name "Helio Retail" (single-word first token derives account_key "helio"; never two-word/punctuated per anti-pattern). Model the CONTRACTION arc per D-01/D-03 and PATTERNS pt4: usage RISES to a peak then FALLS (e.g. 6.0 to 12.0 peak to 4.5 trough) with shrinking cluster_nodes; a downgrade ContractEvent (lower value_usd, auto_renew=False); a closed-lost expansion AND a slipped/at-risk renewal opportunity; NPS where BOTH score AND verbatim_sentiment decline together (8/positive to 3/negative) — the load-bearing D-03 distinction from Q12 (Meridian is green-score/red-sentiment; C is honest decline, no contradiction). Build _signal_docs (questions_served Q13/Q14/Q15), _near_miss_docs (same-vocab positive-outcome distractors), and _noise_docs. Signal-doc vocabulary MUST center on contraction/downgrade/migration-away/declining-usage/deprioritization — lexically distinct from Meridian renewal-pricing-objection/champion-quiet and Northwind scale-limit/GenAI (Pitfall 3). All entity_id via canonical_uuid("helio", ...); all file_name via make_file_name; all citable_url via make_citable_url("helio", source, event_id). The e-commerce/retail vertical name + exact churn prose wording is Claude discretion (D-02), constrained to be non-confusable with Analytics/Logistics.
    In generate.py: add the spine_helio import after the meridian import, and extend _SPINES to [NORTHWIND_SPINE, MERIDIAN_SPINE, HELIO_SPINE]. No other edits (loops iterate _SPINES + MODULE_NAMES generically).
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && python -c "from data_gen.spine.spine_helio import HELIO_SPINE; from data_gen.spine.entity_registry import HELIO_ACCOUNT_ID, MODULE_NAMES; assert HELIO_SPINE.account_name.lower().split()[0]=='helio'; assert {'helio_slack','helio_email','helio_docs','helio_pdf'}.issubset(set(MODULE_NAMES)); qv=[u.query_volume_m for u in HELIO_SPINE.usage]; assert qv[-1] < max(qv); ns=[n.score for n in HELIO_SPINE.nps]; assert ns[-1] < ns[0]; print('OK helio spine declining arc')"</automated>
  </verify>
  <acceptance_criteria>
    - spine_helio imports without error; HELIO_SPINE.account_name == "Helio Retail" (account_key derives to "helio").
    - HELIO_ACCOUNT_ID equals canonical_uuid("helio","helio_retail"); MODULE_NAMES in entity_registry.py contains all 4 helio_ entries.
    - HELIO_SPINE.usage is rise-then-fall (last value below peak) and HELIO_SPINE.nps has score AND sentiment both declining (last score below first; last sentiment "negative") — the D-03 distinction asserted by the verify command passing (NOT skipped).
    - generate.py _SPINES has exactly 3 entries ending in HELIO_SPINE.
  </acceptance_criteria>
  <done>spine_helio.py builds a declining-arc HELIO_SPINE; entity_registry + generate.py wired; the verify command prints "OK helio spine declining arc".</done>
</task>

<task type="auto">
  <name>Task 2: Make generators + structured loader helio-aware (lexicon + loadability)</name>
  <files>data_gen/generators/slack_generator.py, data_gen/generators/email_generator.py, data_gen/generators/docs_generator.py, data_gen/generators/pdf_generator.py, scripts/load_structured.py</files>
  <read_first>
    - data_gen/generators/slack_generator.py — _get_prohibited_terms (lines 27-44; dispatches on module == "meridian_slack" else NORTHWIND) and _derive_concern_topic (lines 89-101; branches on questions_served Q12/Q2/Q9/Q5/Q8 then a generic default).
    - data_gen/generators/email_generator.py — _get_prohibited_terms (lines 39-42; dispatches module.startswith("meridian") else NORTHWIND) and _derive_concern_description (lines 81-113; Q-id branches).
    - data_gen/generators/docs_generator.py — _get_prohibited_terms (lines 34-37) and _derive_event_summary / _derive_key_facts (Q-id branches).
    - data_gen/generators/pdf_generator.py — _get_prohibited_terms (lines 47-50) and _derive_event_summary / _derive_key_facts (Q-id branches).
    - scripts/load_structured.py — SOURCE_FILES dict (lines 46-72): a HARDCODED 12-file list (2 accounts x 6 files). NOT account-agnostic (research assumption A2 was WRONG) — plus _VALID_PRODUCTS (line 78).
    - .planning/phases/09-data-depth-3rd-account/09-PATTERNS.md generators section (prohibited-terms + Q-branch guidance).
  </read_first>
  <action>
    CRITICAL: every unstructured generator _get_prohibited_terms currently falls through to the NORTHWIND list for any non-Meridian module — so an unhandled helio module would be filtered against Northwind prohibited terms (which include "renewal risk","at risk","escalation"), STRIPPING Helio churn signal vocabulary. In all four generators (slack/email/docs/pdf): add a _HELIO_SOURCE_PROHIBITED list (noise-only prohibitions that EXCLUDE Helio signal vocabulary downgrade/contraction/migration-away/declining-usage/churn/deprioritization) and a helio branch in _get_prohibited_terms (match module == "helio_slack" for slack to mirror its exact-match style; module.startswith("helio") for email/docs/pdf to mirror theirs).
    In all four generators prose _derive_ helpers (_derive_concern_topic / _derive_concern_description / _derive_event_summary + _derive_key_facts), add a Q13 branch (and Q14 if the follow-up needs distinct prose) returning contraction-themed copy (declining usage, plan downgrade, migration-away discussion, remediation/save-plan) BEFORE the generic default — otherwise C signal docs render generic prose with no churn lexicon and the near-miss guard fails. Keep the structured-only anchor (Q15) out of these doc-prose branches (it has no signal docs).
    In scripts/load_structured.py: append Helio 6 structured input files to SOURCE_FILES — helio/crm/helio_crm_accounts.json (Account), helio/crm/helio_crm_contacts.json (Contact), helio/crm/helio_crm_opportunities.json (Opportunity), helio/crm/helio_crm_nps.json (NPS), helio/snowflake/helio_snowflake_usage_metrics.json (UsageFact), helio/docusign/helio_docusign_contracts.json (Contract). Update the header comment from "2 accounts" to "3 accounts". (Without this, C structured records never load — DATA-04 cannot be satisfied.) Confirm C downgrade product_scope is within _VALID_PRODUCTS (Community,Enterprise,ArangoGraph,GenAI); if outside, adjust the spine in Task 1 rather than widening the validator.
    VERIFY ORDERING (must hold): write scripts/verify_helio_generators.py (the throwaway probe described in the acceptance criteria) as the FINAL step of this action, BEFORE running the <automated> verify command — the verify command invokes that probe, so it cannot run until the probe exists.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && python scripts/verify_helio_generators.py</automated>
  </verify>
  <acceptance_criteria>
    - For each of helio_slack/email/docs/pdf, _get_prohibited_terms returns a list that does NOT contain Helio signal vocabulary (downgrade/contraction/migration/declining/churn) — proves a real helio branch exists, not the Northwind fall-through. (Write scripts/verify_helio_generators.py as a tiny throwaway probe that constructs a DocEvent per module and asserts this, plus asserts load_structured SOURCE_FILES contains exactly 6 helio/ paths; it must print "OK generators + loader helio-aware" and exit 0.)
    - scripts/load_structured.py SOURCE_FILES contains exactly 6 helio/ file paths (one per entity type).
    - Each generator prose _derive_ helper has a Q13 branch (grep "Q13" in each of the 4 generator files returns a match inside a _derive_ function).
  </acceptance_criteria>
  <done>All 4 unstructured generators dispatch helio modules to a helio-specific prohibited list + Q13 prose branch; load_structured registers C 6 files; verify probe prints "OK generators + loader helio-aware".</done>
</task>

<task type="auto">
  <name>Task 3: Wire C's 2-3 questions into linter + locked eval gate</name>
  <files>data_gen/linter/conftest.py, data_gen/linter/test_answerability.py, data_gen/linter/test_near_miss_guard.py, agent/test/questions.eval.test.ts, agent/src/index.ts, scripts/eval-gate.ts</files>
  <read_first>
    - data_gen/linter/conftest.py — duplicated MODULE_NAMES (lines 16-25, NOT imported from entity_registry) + QUESTION_IDS (line 27).
    - data_gen/linter/test_answerability.py — per-question test fns (Q7 structured-only lines 60-80; Q2 dual lines 88-101; Q12 all-module lines 108-128); reusable helpers _signal_docs_for/_has_records (lines 25-52); test_both_accounts_have_structured_data (lines 212-227).
    - data_gen/linter/test_near_miss_guard.py — QUESTION_TEXTS (lines 55-80); test_near_miss_guard_q12 (lines 221-234, the exact shape to mirror, scoped to one account modules); _require_api_key (88-91); _assert_signal_top1 (176-213).
    - agent/test/questions.eval.test.ts — dual-graph it (Q2 lines 127-150); structured-only anchor it (Q7 lines 100-125); Q12 regex answer assertion (lines 153-181); module constants FAITHFULNESS_FLOOR/TIMEOUT/d (lines 59-84).
    - agent/src/index.ts — Q7_ANCHOR_PROMPT single-source-of-truth pattern (lines 41-45).
    - scripts/eval-gate.ts — LOCKED_QUESTION_LABELS (line 157) + classifyTest substring scan (lines 159-177).
  </read_first>
  <action>
    Assign C question IDs Q13 (flagship dual-graph "Is Helio Retail at risk of churning, and why?"), Q14 (follow-up — remediation/save-plan or decline timeline), Q15 (structured-only anchor mirroring Q7 role for C) per D-04. Use 2 or 3 per Claude discretion; if you ship 2, use Q13+Q15 and drop Q14 everywhere consistently.
    conftest.py: append the 4 helio modules to the DUPLICATED MODULE_NAMES (must stay in sync with entity_registry or test_module_names_valid fails) and append the C question IDs to QUESTION_IDS.
    test_answerability.py: add a dual-graph signal-doc-present test for Q13 (mirror test_q2_signal_docs_present, swap module list to helio and question to "Q13"); add a structured-records-present test for the C anchor Q15 (mirror test_q7_structured_records_present, swap "northwind" to "helio"); add Q14 if used. Extend test_both_accounts_have_structured_data to include "helio" (rename to test_all_accounts_have_structured_data).
    test_near_miss_guard.py: add a Q13 entry to QUESTION_TEXTS using vocabulary present in Helio signal docs (contraction/downgrade/migration-away); add test_near_miss_guard_q13 scoped to helio modules (mirror test_near_miss_guard_q12). Add a Q14 guard only if Q14 is dual-graph with its own signal docs.
    agent/src/index.ts: if the C anchor prompt needs cross-file reuse (web canary), add an exported QC_ANCHOR_PROMPT constant following the Q7_ANCHOR_PROMPT discipline; otherwise inline the prompt in the test.
    questions.eval.test.ts: add it blocks for C inside the existing d(...) describe block — Q13 dual-graph (assertReconciliation(env) true, not refused, groundingScore 1.0, faithfulness at/above FAITHFULNESS_FLOOR, answer matches /declin|contract|downgrad|churn|at.risk/i); Q15 structured-only anchor (every citation.graph is 'structured', not refused, groundingScore 1.0, faithfulness floor); Q14 follow-up if used. Each it label string MUST literally contain its Q-id (e.g. 'Q13 — helio churn / contraction [dual-graph]') so eval-gate classifies it as locked.
    scripts/eval-gate.ts: append C labels to LOCKED_QUESTION_LABELS (e.g. add Q13, Q14, Q15) so the new tests classify as locked, not other.
    VERIFY ORDERING (must hold): write scripts/verify_helio_questions.py (the throwaway probe described in the acceptance criteria) as the FINAL step of this action, BEFORE running the <automated> verify command — the verify command invokes that probe, so it cannot run until the probe exists.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && pytest data_gen/linter/test_field_stamps.py data_gen/linter/test_answerability.py -k "helio or all_accounts or module_names" -v 2>&1 | tail -20 && python scripts/verify_helio_questions.py</automated>
  </verify>
  <acceptance_criteria>
    - conftest QUESTION_IDS and entity_registry+conftest MODULE_NAMES both include C question IDs and 4 helio modules; test_field_stamps::test_module_names_valid passes (asserted by the pytest run above — must show passed/skipped, never failed/errored).
    - test_near_miss_guard.py defines test_near_miss_guard_q13 (and Q14 if dual) and QUESTION_TEXTS has a Q13 key. (Write scripts/verify_helio_questions.py probe: assert each of eval-gate.ts LOCKED_QUESTION_LABELS, questions.eval.test.ts, test_near_miss_guard.py QUESTION_TEXTS, and conftest QUESTION_IDS contains Q13; print "OK Q13 wired across gate" exit 0.)
    - eval-gate.ts LOCKED_QUESTION_LABELS and questions.eval.test.ts both literally contain the new Q-id label strings.
  </acceptance_criteria>
  <done>C questions wired into conftest, answerability, near-miss guard, the locked eval test, and eval-gate labels; pytest field-stamps + answerability helio tests green; verify probe prints "OK Q13 wired across gate".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated synthetic data to KG/agent | New Account C facts (100% synthetic) cross into the retrieval corpus and the locked eval gate. The risk is not auth or injection — it is GROUNDING INTEGRITY: a C fact that the answer asserts but cannot trace to a C record, or a C doc that out-ranks the wrong account, would be a confident-wrong-answer in front of Zscaler. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | spine_helio.py synthetic data | accept | 100% synthetic e-commerce account; no PII, no real customer data (CLAUDE.md constraint). No control needed. |
| T-09-02 | Tampering (grounding) | C signal docs vs near-miss distractors under RRF | mitigate | Empirical near-miss guard (test_near_miss_guard_q13) asserts C signal doc ranks top-1 over C distractors; deterministic keys (canonical_uuid/make_file_name/make_citable_url) make every C fact traceable. Lexicon kept distinct per D-03 so C does not poison N/M retrieval (Pitfall 3). |
| T-09-03 | Tampering (false-green) | empirical guard silently skips without OPENAI_API_KEY + AUTOGRAPH_PATH | mitigate | Acceptance criteria assert the guard RAN (Plan 03 greps pytest output for skipped on near-miss tests); this plan only authors the test, Plan 03 executes it under the env-guard. |
| T-09-04 | Spoofing/Repudiation | cross-account entity_id collision | mitigate | Per-account canonical_uuid("helio", ...) scope prevents C entity_id colliding with N/M (RESEARCH Pitfall 5 / entity_registry.py:46). |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH Package Legitimacy Audit: N/A). No install task to gate. |
</threat_model>

<verification>
- spine_helio importable; declining usage + declining NPS asserted (Task 1 verify).
- generators dispatch helio modules to a helio prohibited list (not Northwind fall-through) and load_structured registers C 6 files (Task 2 verify).
- C questions wired into conftest/answerability/near-miss-guard/eval-test/eval-gate; field-stamps + answerability helio tests green (Task 3 verify).
- Quick linter after each commit: `pytest data_gen/linter/ -k "not near_miss"` (fast, no API) per VALIDATION sampling rate.
- NOTE: full regen, full near-miss-guard execution, KG rebuild, and eval-gate GREEN are Plan 03 (this plan authors data + tests only; it does not regenerate output/ or ingest).
</verification>

<success_criteria>
- DATA-04 building blocks complete: Account C spine (contraction arc, D-01/D-03), distinct vertical (D-02), 4 modules in both MODULE_NAMES + load_structured, helio-aware generators, and 2-3 locked C questions wired into linter + eval gate + near-miss guard.
- All Task verify commands pass (none skipped).
- `pytest data_gen/linter/ -k "not near_miss"` collects without errors (data not yet generated → fixtures skip cleanly, but module/question coupling tests run on constants).
</success_criteria>

<output>
Create `.planning/phases/09-data-depth-3rd-account/09-01-SUMMARY.md` when done.
Delete the throwaway probes scripts/verify_helio_generators.py and scripts/verify_helio_questions.py (or note them in the summary) — they are scaffolds, not shipped code.
</output>
