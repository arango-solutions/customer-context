---
phase: 09-data-depth-3rd-account
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - data_gen/generators/slack_generator.py
  - data_gen/generators/email_generator.py
  - data_gen/generators/docs_generator.py
  - data_gen/generators/pdf_generator.py
  - data_gen/generators/crm_generator.py
  - data_gen/generators/usage_generator.py
  - data_gen/generators/contract_generator.py
autonomous: true
requirements: [DATA-05]

must_haves:
  truths:
    - "Prose realism + record richness are broadly deepened across all 8 account x source modules for BOTH existing accounts (Northwind + Meridian): crm, contract, usage, nps (structured) + slack, docs, email, pdf (unstructured) — D-05."
    - "Deepening is in-place CONTENT enrichment of EXISTING DocEvents and records (longer/more-specific/cross-referenced prose; richer structured fields) — NOT new DocEvents to existing modules, which would inflate near-miss distractor count and risk flipping existing guards to RED."
    - "No new ungrounded/hallucination surface is introduced: every enriched claim still traces to a spine fact; the lane intent (EDIT vs ADD) is documented per changed module for Phase 12 CDC."
  artifacts:
    - path: "data_gen/generators/slack_generator.py"
      provides: "Deeper Northwind+Meridian slack prose via enriched _derive_concern_topic / _build_facts_for_signal"
      contains: "_derive_concern_topic"
    - path: "data_gen/generators/docs_generator.py"
      provides: "Deeper docs prose via enriched _derive_event_summary / _derive_key_facts"
      contains: "_derive_key_facts"
    - path: "data_gen/generators/crm_generator.py"
      provides: "Richer structured CRM records (additional grounded fields)"
      contains: "accounts_records"
  key_links:
    - from: "data_gen/generators/_generator.py"
      to: "prose-hash cache (data_gen/llm/prose_client.py)"
      via: "editing prompt/template busts the hash; --clean regen recomputes"
      pattern: "_derive_"
---

<objective>
Broadly deepen the prose realism and record richness across ALL 8 account x source modules for the two EXISTING accounts (Northwind + Meridian), per D-05 — making cited answers more impressive without introducing any new ungrounded surface and without regressing the linter or the Phase 8 eval gate. Deepening edits the generators' prose templates / _derive_ helper functions (unstructured) and the record dicts (structured), favoring in-place content enrichment over adding new DocEvents (Pattern 3 — adding docs inflates near-miss distractors).

Purpose: DATA-05 — richer, more coherent data so answers and (downstream) the viz are more compelling. Scaled-up scope per the user's explicit "ambitious" direction (CONTEXT specifics).
Output: Enriched generator prose + structured record fields. This plan does NOT regen output/ or ingest (that is Plan 03) — it changes the generators only; the regen in Plan 03 busts the prose cache and materializes the deepened content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-depth-3rd-account/09-CONTEXT.md
@.planning/phases/09-data-depth-3rd-account/09-RESEARCH.md
@.planning/phases/09-data-depth-3rd-account/09-PATTERNS.md
@.planning/phases/09-data-depth-3rd-account/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepen unstructured prose for Northwind + Meridian (slack, email, docs, pdf)</name>
  <files>data_gen/generators/slack_generator.py, data_gen/generators/email_generator.py, data_gen/generators/docs_generator.py, data_gen/generators/pdf_generator.py</files>
  <read_first>
    - data_gen/generators/slack_generator.py — _derive_concern_topic (lines 89-101) + _build_facts_for_signal (lines 47-72): the prose richness sites for slack.
    - data_gen/generators/email_generator.py — _derive_concern_description (lines 81-113) + _derive_subject_context (lines 114-143).
    - data_gen/generators/docs_generator.py — _derive_event_summary + _derive_key_facts (key_facts is a list of strings; each item becomes a paragraph in the generated .md) (lines 75-175).
    - data_gen/generators/pdf_generator.py — _derive_event_summary + _derive_key_facts (lines 66-100).
    - data_gen/spine/spine_northwind.py and data_gen/spine/spine_meridian.py — the SOURCE OF TRUTH for every fact the prose may reference (contract values, dates, contact names, usage figures). Prose must only assert facts present here.
    - .planning/phases/09-data-depth-3rd-account/09-PATTERNS.md Pattern 3 (EDIT vs ADD lane) + Pitfall 4 (prose-cache bust / stub fallback).
  </read_first>
  <action>
    For each existing per-question branch in the prose _derive_ helpers (Q12/Q2/Q9/Q5/Q8 in slack, email, docs, pdf), enrich the returned strings: make them longer, more specific, and cross-referenced — incorporate concrete grounded details that already exist in the spine (named contacts, the specific contract value_usd and product tier, the relevant usage figures, dates). For docs/pdf _derive_key_facts, ADD MORE list items (each is one paragraph of grounded structured content) without inventing facts absent from the spine. Do this for BOTH Northwind and Meridian (the helpers dispatch by module prefix / questions_served, so enriching a branch deepens both accounts where that branch applies).
    Do NOT add new DocEvents to existing modules (that is a spine change and inflates near-miss distractor count — Pattern 3 / anti-pattern). Do NOT touch the helio Q13/Q14 branches added in Plan 01 except to keep style consistent. Do NOT loosen any _get_prohibited_terms list (noise docs must stay signal-free).
    Determinism caveat (Pitfall 4): editing these templates changes the prompt hash, so Plan 03 --clean regen will make real LLM calls for every deepened doc. Keep prose grounded and bounded so the completion summary does not report stub fallbacks and cross-source-agreement/chronology stay green. Document the lane intent per module (these are all EDIT-lane content edits — membership stable, no module re-cluster) in the SUMMARY so Phase 12 CDC inherits a clean delta map (D-07.2).
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && python -c "import importlib; [importlib.import_module('data_gen.generators.'+m) for m in ('slack_generator','email_generator','docs_generator','pdf_generator')]; print('OK 4 unstructured generators import clean')"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 unstructured generators import without error (verify command prints "OK 4 unstructured generators import clean").
    - Each enriched _derive_ helper still references only spine-present facts (manual spot check during review; the cross-source-agreement linter in Plan 03 enforces this empirically after regen).
    - No new DocEvent added to any existing Northwind/Meridian module (git diff touches only the 4 generator files, not any spine_*.py).
    - helio Q13/Q14 prose branches from Plan 01 are unchanged (git diff shows no removal of helio branches).
  </acceptance_criteria>
  <done>The 4 unstructured generators have richer, still-grounded per-question prose for N+M; imports clean; no new DocEvents; lane intent documented in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen structured records for Northwind + Meridian (crm, usage, contract)</name>
  <files>data_gen/generators/crm_generator.py, data_gen/generators/usage_generator.py, data_gen/generators/contract_generator.py</files>
  <read_first>
    - data_gen/generators/crm_generator.py — generate_crm (line 32); accounts_records (line 65), contacts_records (84), opportunities_records (104), nps_records (129): the record dicts to enrich.
    - data_gen/generators/usage_generator.py — generate_usage (line 21) + records.append block (lines 41-55): the usage record dict.
    - data_gen/generators/contract_generator.py — generate_contracts (line 18): the contract record dict.
    - scripts/load_structured.py — the loader/validators that consume these JSON files (do NOT add a field the loader will reject; structured ingest is idempotent AQL UPSERT). Check _VALID_PRODUCTS and any required-key assertions.
    - data_gen/linter/test_field_stamps.py + test_referential_integrity.py — the structural contracts new fields must not violate (deterministic stamps, FK integrity).
  </read_first>
  <action>
    Enrich the structured record dicts (crm accounts/contacts/opportunities/nps, usage, contract) with additional GROUNDED fields that add demo realism — e.g. on accounts: industry, region, csm_owner, tier, health_score_detail; on opportunities: richer stage history or competitor context; on usage: derived growth_rate context already present in the series; on contracts: payment_terms / seat_count consistent with value_usd. Every added field must be deterministic (derived from the spine or Faker.seed(42), never random) and must not contradict an existing spine fact (cross-source-agreement linter enforces this in Plan 03). Apply to BOTH Northwind and Meridian (generators are single-spine and auto-iterate _SPINES, so the same code deepens both; it will ALSO deepen Helio's structured records identically — that is acceptable and desirable).
    Do not change record COUNTS or _key derivation (referential integrity + field stamps depend on them). Do not add a field that scripts/load_structured.py asserts against or rejects — extra descriptive fields the loader ignores are fine; structural keys are not.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && python -c "import importlib; [importlib.import_module('data_gen.generators.'+m) for m in ('crm_generator','usage_generator','contract_generator')]; print('OK 3 structured generators import clean')"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 structured generators import without error (verify command prints "OK 3 structured generators import clean").
    - Added fields are deterministic (sourced from spine or Faker.seed(42)) — no uuid4/random.* introduced (grep the 3 files for "uuid4" and "random." returns no NEW occurrence).
    - Record counts and _key derivation unchanged (git diff does not alter the list-comprehension iteration source or key fields).
    - Structural linters stay green after regen (enforced in Plan 03 via test_field_stamps + test_referential_integrity).
  </acceptance_criteria>
  <done>crm/usage/contract generators emit richer grounded fields for all accounts; imports clean; counts/keys unchanged; no nondeterminism introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deepened synthetic prose to KG/agent | Enriched Northwind/Meridian prose crosses into the retrieval corpus consumed by the locked eval gate. The risk is GROUNDING INTEGRITY: deeper prose that asserts a fact NOT in the spine becomes an ungrounded claim the answer could surface — a confident-wrong-answer risk in front of Zscaler. No auth/injection surface (synthetic data, no untrusted input). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09D-01 | Tampering (grounding) | enriched _derive_ prose for existing Qs | mitigate | Prose enrichment constrained to spine-present facts only; cross-source-agreement + chronology linters (Plan 03) flag any drift; faithfulness floor 0.6 + enforceGrounding _id gate (eval gate, Plan 03) reject ungrounded claims. |
| T-09D-02 | Tampering (regression) | deepened N/M docs flipping an existing near-miss guard to RED | mitigate | Prefer EDIT-lane content edits (no new DocEvents); re-run all 5 existing N/M near-miss guards in Plan 03 to confirm no flip (Pitfall 3). |
| T-09D-03 | Tampering (nondeterminism) | added structured fields | mitigate | All added fields derived from spine or Faker.seed(42); deterministic-key helpers unchanged; field-stamps linter enforces stamp format (Plan 03). |
| T-09D-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Both tasks: generators import clean (per-task verify).
- After each commit: `pytest data_gen/linter/ -k "not near_miss"` (fast, no API) per VALIDATION sampling.
- NOTE: full regen (which materializes the deepened prose and busts the cache), the full near-miss-guard re-run, and eval-gate GREEN are Plan 03. This plan changes generators only.
- Watch the regen completion summary (Plan 03) for stub fallbacks — a stub indicates a deepened prompt produced thin filler that may not clear faithfulness (Pitfall 4).
</verification>

<success_criteria>
- DATA-05 satisfied at the generator level: all 8 modules for Northwind + Meridian have deeper prose / richer records, grounded and deterministic.
- No new DocEvents added to existing modules; no nondeterminism; record counts/keys unchanged.
- All per-task verify commands pass.
</success_criteria>

<output>
Create `.planning/phases/09-data-depth-3rd-account/09-02-SUMMARY.md` when done.
Record in the SUMMARY the EDIT-lane-vs-ADD-lane intent per changed module (all EDIT-lane content edits this plan) for Phase 12 CDC.
</output>
