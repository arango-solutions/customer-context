---
phase: 09-data-depth-3rd-account
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - data_gen/output/
  - build_manifest.json
autonomous: false
requirements: [DATA-04, DATA-05]
user_setup:
  - service: openai
    why: "Prose generation, near-miss-guard embeddings, faithfulness scoring in eval gate"
    env_vars:
      - name: OPENAI_API_KEY
        source: "repo-root .env (already present)"
  - service: arangodb
    why: "Structured load + KG rebuild + eval gate live queries"
    env_vars:
      - name: ARANGO_ENDPOINT
        source: "repo-root .env (already present)"
  - service: autograph
    why: "Near-miss-guard RRF import (AUTOGRAPH_PATH) + KG orchestrate (AUTOGRAPH_URL)"
    env_vars:
      - name: AUTOGRAPH_PATH
        source: "shell export — NOT in .env (only AUTOGRAPH_URL is). Export /Users/plosiewicz/Desktop/autograph before the linter, or the near-miss guard SILENTLY SKIPS (false green)."

must_haves:
  truths:
    - "A single full --clean regen materializes all 3 accounts (N, M deepened + C new) across all 12 modules deterministically (SC-1)."
    - "The full 6-test linter passes — including the EMPIRICAL near-miss guard which is asserted to have RUN (not skipped), with C's Q13 guard top-1 and all 5 existing N/M guards still green (SC-2, no Pitfall-3 regression)."
    - "Structured load (C's records via the widened SOURCE_FILES) + a single serialized full KG rebuild (Option A, --build-id resume) ingest all 12 modules (SC-3)."
    - "The Phase 8 eval gate (npx tsx scripts/eval-gate.ts) is GREEN — the 6 existing answers are not corrupted AND C's new locked questions answer end-to-end (SC-3, SC-4, SC-5)."
  artifacts:
    - path: "data_gen/output/manifest.json"
      provides: "Regenerated manifest covering all 12 modules incl. helio_*"
      contains: "helio_slack"
    - path: "data_gen/output/structured/helio"
      provides: "Account C structured JSON (crm/snowflake/docusign)"
  key_links:
    - from: "data_gen/generate.py --clean"
      to: "data_gen/output/ (all 12 modules)"
      via: "regen busts prose cache, materializes deepened + C prose"
      pattern: "helio"
    - from: "scripts/build_unstructured.py"
      to: "ArangoDB customer360 KG"
      via: "full rebuild (Option A), serialized one build/orchestrate"
      pattern: "build_unstructured"
    - from: "scripts/eval-gate.ts"
      to: "live agent + ArangoDB + OpenAI"
      via: "locked-question classification GREEN (SC-5)"
      pattern: "LOCKED_QUESTION_LABELS"
---

<objective>
Materialize and validate everything from Plans 01 + 02: pre-flight the environment, run ONE full --clean regen, run the full 6-test linter (asserting the empirical near-miss guard RAN), load C's structured records, run ONE serialized full unstructured KG rebuild (Option A per RESEARCH — the as-shipped build_unstructured.py does a full rebuild, NOT a per-module add; --build-id resume mitigates wall-clock), then confirm the Phase 8 eval gate stays GREEN. This closes SC-1 through SC-5 for DATA-04 + DATA-05.

Purpose: The hard gate. The linter is the pre-ingest gate; the eval gate is the post-change gate (SC-5 — existing answers must not be corrupted, and C's new questions must answer end-to-end).
Output: Regenerated output/ (12 modules), a rebuilt KG, and a GREEN eval gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-data-depth-3rd-account/09-CONTEXT.md
@.planning/phases/09-data-depth-3rd-account/09-RESEARCH.md
@.planning/phases/09-data-depth-3rd-account/09-VALIDATION.md
@.planning/spikes/001-autograph-kg-claim-sourcing/UPDATE-PIPELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight + full regen + full linter (HARD GATE, near-miss guard must RUN)</name>
  <files>data_gen/output/</files>
  <read_first>
    - data_gen/generate.py — the --clean flag behavior (lines 167-179: prose stats real_llm/cache_hit/stub counts) and the regen entrypoint.
    - data_gen/linter/test_near_miss_guard.py — _require_api_key skip guard (lines 88-91) + the RRF import requiring AUTOGRAPH_PATH (lines 38-41); confirms WHY the guard skips silently without env (Pitfall 1, the biggest false-green risk this phase).
    - .planning/phases/09-data-depth-3rd-account/09-RESEARCH.md Runtime State Inventory (iCloud " 2"/" 3" dup-dir cleanup) + Pitfalls 1,4,5.
    - memory icloud-desktop-sync-conflicts + openai-key-env-gotcha.
  </read_first>
  <action>
    PRE-FLIGHT (per VALIDATION Wave 0): (a) delete any iCloud duplicate dirs under data_gen/output/structured/ matching " 2"/" 3" suffixes (e.g. "meridian 2", "northwind 3") — they pollute the linter's structured.glob and the FM upload set (Pitfall 5). (b) Export AUTOGRAPH_PATH=/Users/plosiewicz/Desktop/autograph in the shell — it is NOT in .env (only AUTOGRAPH_URL is); without it the near-miss guard silently skips (Pitfall 1). (c) Confirm OPENAI_API_KEY and ARANGO_* resolve from repo-root .env (entrypoints already use load_dotenv override=True — do not add an entrypoint that omits it, Pitfall 2).
    Run a single full deterministic regen: `python data_gen/generate.py --clean`. This wipes the prose cache and materializes the deepened N/M prose + all of C's data across 12 modules. Inspect the completion summary's real_llm/cache_hit/stub counts; investigate any stub fallbacks (a failed LLM call producing thin filler — Pitfall 4).
    Run the full linter as the HARD GATE with AUTOGRAPH_PATH exported: `AUTOGRAPH_PATH=/Users/plosiewicz/Desktop/autograph pytest data_gen/linter/ -v`. All 6 tests must pass. CRITICAL: the near-miss guard tests (test_near_miss_guard_q13 + the 5 existing N/M guards) must show PASSED, never skipped — if any near-miss test is marked skipped (s), the empirical gate did not run (false green) and the task is NOT done.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && AUTOGRAPH_PATH=/Users/plosiewicz/Desktop/autograph python scripts/run_linter_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - data_gen/output/manifest.json exists and includes helio_slack/email/docs/pdf entries; data_gen/output/structured/helio/ exists with crm/snowflake/docusign JSON.
    - No "meridian 2"/"northwind 3"-style duplicate dirs remain under data_gen/output/structured/.
    - The full linter is GREEN AND the near-miss guard tests RAN (NOT skipped). Write scripts/run_linter_gate.py: it runs `pytest data_gen/linter/ -v`, parses the summary, and exits non-zero if (a) any test failed/errored OR (b) any test name containing "near_miss" is marked skipped — printing the offending test names. This makes "guard silently skipped" a hard failure (Pitfall 1 / VALIDATION manual-only check #2 automated).
    - Regen completion summary reports zero stub fallbacks (or any stubs are investigated and justified in the SUMMARY).
  </acceptance_criteria>
  <done>output/ regenerated for 12 modules; iCloud junk cleaned; full linter GREEN with near-miss guard PROVEN to have run (scripts/run_linter_gate.py exits 0).</done>
</task>

<task type="auto">
  <name>Task 2: Structured load + serialized full KG rebuild (Option A)</name>
  <files>build_manifest.json</files>
  <read_first>
    - scripts/load_structured.py — the idempotent AQL UPSERT loader; confirm it now reads C's 6 files from the SOURCE_FILES widened in Plan 01 Task 2.
    - scripts/build_unstructured.py — the full-rebuild ingest (Stage 1 FM upload lines 129-151; Stage 2 create_corpus_build incremental=False lines 154-162; Stage 3-4 strategize + orchestrate_with_wait no partition_ids lines 188-203; Stage 7 repair_kg_attribution lines 311-325); the --build-id resume + --skip-orchestrate flags.
    - lib/autograph_client.py — orchestrate_with_wait 409 retry/serialization (lines 541-587); confirms only ONE build/orchestrate at a time (Pitfall 6).
    - .planning/spikes/001-autograph-kg-claim-sourcing/UPDATE-PIPELINE.md — why Option A (full rebuild) is the safe Phase-9 path; isolated ADD lane is Phase 12 CDC, NOT here.
    - memory autograph-creds-provisioning + autograph-filename-scramble (why attribution is content-derived, not manifest-keyed).
  </read_first>
  <action>
    Load C's structured records: `python scripts/load_structured.py` (idempotent UPSERT; account-agnostic loader now includes Helio's 6 files from Plan 01).
    Run a SINGLE serialized full unstructured KG rebuild (Option A — RESEARCH: the as-shipped script does a full rebuild over the whole manifest; do NOT attempt the per-module ADD lane, which is unverified and is Phase 12 territory): `python scripts/build_unstructured.py` with no --modules filter, so it rebuilds all 12 modules fresh. SERIALIZE — never launch a second build/orchestrate concurrently (409 OrchestrationInProgressError, Pitfall 6). If the rebuild is interrupted, resume with the --build-id of the in-flight build rather than restarting (wall-clock mitigation, RESEARCH Open Question 2 / assumption A3). Stage 7 attribution (repair_kg_attribution.py) is content-derived because AutoGraph scrambles Document.file_name — let the script handle it; do not switch to manifest-keyed attribution.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && python scripts/verify_kg_loaded.py</automated>
  </verify>
  <acceptance_criteria>
    - load_structured.py completes; the structured graph contains Helio's Account/Contact/Opportunity/NPS/UsageFact/Contract records (verify via an AQL count of helio-scoped vertices).
    - build_unstructured.py completes one full rebuild; the KG _Documents collection contains helio_* docs. Write scripts/verify_kg_loaded.py: connect via ARANGO_* (load_dotenv override=True), assert (a) helio structured vertices exist (count > 0) and (b) the KG document collection has docs attributed to helio modules; print "OK KG has all 3 accounts" exit 0, else non-zero.
    - Only one build/orchestrate ran (no 409 left unresolved in the build log).
  </acceptance_criteria>
  <done>C structured records loaded; full KG rebuilt for all 12 modules in one serialized pass; verify_kg_loaded.py confirms all 3 accounts present; exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: Eval gate GREEN (SC-5) — existing answers intact + C answers end-to-end</name>
  <files>build_manifest.json</files>
  <read_first>
    - scripts/eval-gate.ts — the GREEN/RED classifier (LOCKED_QUESTION_LABELS now includes C's Q-ids from Plan 01); the pre-demo command.
    - agent/test/questions.eval.test.ts — the locked questions incl. C's new it() blocks (Q13 dual / Q15 anchor); FAITHFULNESS_FLOOR 0.6; assertReconciliation.
    - .planning/phases/09-data-depth-3rd-account/09-VALIDATION.md Per-Task Verification Map (DATA-04/05 + SC-5 rows).
  </read_first>
  <action>
    Run the Phase 8 eval gate against the live, rebuilt KG: `npx tsx scripts/eval-gate.ts`. It must be GREEN — the 6 existing locked questions (Q7/Q2/Q12/Q9/Q5/Q8) must still pass (SC-5: not corrupted by the deepened data or the new account) AND C's new locked questions (Q13 dual-graph, Q15 structured-only anchor, Q14 if shipped) must answer end-to-end: not refused, groundingScore 1.0, faithfulness at/above 0.6, dual-graph reconciliation true for Q13, all-structured citations for Q15.
    If any existing question regressed (e.g. Q2/Q12 faithfulness dropped — the Pitfall-3 lexical-bleed signal), STOP and return a gap rather than loosening the gate: the cause is C's lexicon colliding with N/M retrieval and must be fixed in the spine/prose (Plan 01/02), not by weakening the eval gate. The eval gate and faithfulness floor are LOCKED (D-06) — never edit them to pass.
  </action>
  <verify>
    <automated>cd /Users/plosiewicz/Desktop/customer360 && npx tsx scripts/eval-gate.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/eval-gate.ts` exits GREEN (exit 0) with the live OpenAI + ArangoDB env (NOT skipped — the describe.skip path means env was absent; that is a false green and is NOT done).
    - All 6 existing locked questions pass (SC-5: existing answers not corrupted).
    - C's new locked questions (Q13 + Q15, and Q14 if shipped) pass: not refused, groundingScore 1.0, faithfulness >= 0.6, Q13 reconciliation true, Q15 all-structured citations (SC-3 end-to-end answerability).
    - The eval gate and FAITHFULNESS_FLOOR were NOT modified to pass (git diff on scripts/eval-gate.ts and questions.eval.test.ts shows no threshold/assertion loosening).
  </acceptance_criteria>
  <done>eval-gate.ts GREEN with live env; 6 existing + C's new locked questions all pass; gate thresholds unchanged. SC-1..SC-5 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated data to live KG to agent answers | This plan ingests all 3 accounts into the live ArangoDB KG and exercises the agent end-to-end. The whole-phase GROUNDING-INTEGRITY risk lands here: a confident-wrong-answer in front of Zscaler is the failure mode to avoid. The controls are the empirical near-miss guard (pre-ingest) and the faithfulness-floor + enforceGrounding _id gate (post-ingest, eval gate). No new auth/injection surface (synthetic data, existing JWT to ArangoDB unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09I-01 | Tampering (false-green) | near-miss guard skips without AUTOGRAPH_PATH/OPENAI_API_KEY | mitigate | scripts/run_linter_gate.py treats any skipped near_miss test as a HARD failure (Task 1) — the single biggest false-green risk this phase (Pitfall 1). |
| T-09I-02 | Tampering (regression) | deepened N/M data corrupts the 6 existing answers | mitigate | eval gate re-run is GREEN-gated on all 6 existing questions (SC-5); a regression returns a gap, never a loosened gate (D-06 locked). |
| T-09I-03 | Tampering (grounding) | C's churn lexicon bleeds into N/M retrieval (Q2/Q12) | mitigate | C lexicon kept distinct (D-03); all 5 existing N/M near-miss guards re-asserted GREEN in Task 1; eval-gate Q2/Q12 faithfulness re-checked in Task 3 (Pitfall 3). |
| T-09I-04 | Denial of Service | concurrent KG builds (409) | mitigate | Serialize ingest (one build/orchestrate); --build-id resume on interruption (Pitfall 6 / autograph_client 409 retry). |
| T-09I-05 | Information Disclosure | live KG content | accept | 100% synthetic data; no PII; existing ArangoDB JWT unchanged. |
| T-09I-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- Task 1: full linter GREEN with near-miss guard PROVEN to have run (run_linter_gate.py exits 0) — SC-2.
- Task 2: structured + KG ingest of all 3 accounts (verify_kg_loaded.py exits 0) — SC-1, SC-3 (data side).
- Task 3: eval-gate.ts GREEN with live env, 6 existing + C's new questions pass — SC-3 (answerability), SC-4, SC-5.
- This is the phase gate: full linter GREEN AND eval-gate.ts GREEN before /gsd-verify-work (VALIDATION sampling: Phase gate).
</verification>

<success_criteria>
- SC-1: 3rd account exists, generated from the canonical spine + entity registry with shared entity_id namespace (materialized in output/).
- SC-2: all new data passes the referential-integrity / timeline linter before ingest (full linter GREEN, near-miss guard RAN).
- SC-3: at least one new C question arc answerable end-to-end structured + unstructured (Q13 dual passes the eval gate).
- SC-4: existing accounts deepened with no linter regression (full linter GREEN after deepening).
- SC-5: the Phase 8 eval gate stays GREEN after loading the new data (existing answers not corrupted).
</success_criteria>

<output>
Create `.planning/phases/09-data-depth-3rd-account/09-03-SUMMARY.md` when done.
Record: regen stub count, linter result (with explicit confirmation the near-miss guard RAN), KG rebuild build-id + wall-clock, and the eval-gate GREEN result mapped to SC-1..SC-5.
Delete throwaway probes scripts/run_linter_gate.py, scripts/verify_kg_loaded.py (or note retention) — scaffolds, not shipped code.
</output>
