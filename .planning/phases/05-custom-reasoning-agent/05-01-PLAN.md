---
phase: 05-custom-reasoning-agent
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - agent/package.json
  - agent/tsconfig.json
  - agent/vitest.config.ts
  - agent/src/envelope.ts
  - agent/src/db.ts
  - agent/src/retrievalPath.ts
  - agent/test/envelope.test.ts
  - agent/test/db.spike.test.ts
  - agent/test/hybridSpike.test.ts
  - agent/test/fixtures.ts
  - .env
  - .env.example
  - .gitignore
autonomous: false
requirements: [AGENT-01, AGENT-03]
user_setup:
  - service: anthropic
    why: "The Opus 4.8 planner + Sonnet/Haiku sub-steps require an Anthropic API key; .env currently only has OPENAI_API_KEY (RESEARCH Pitfall 2, Environment Availability)."
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> Settings -> API Keys (https://console.anthropic.com/settings/keys)"

must_haves:
  truths:
    - "A TypeScript package exists at agent/ with ai@6.0.208, @ai-sdk/anthropic@3.0.85, @ai-sdk/openai@3.0.73, arangojs@10.3.1, zod@4.4.3 pinned and installed."
    - "The Zod EnvelopeSchema ({answer, refused, claims[], citations[], retrievalPath[], reasoningTrace[]}) parses a valid envelope and rejects a malformed one."
    - "An arangojs read against the live customer360 cluster succeeds (basic auth or useBearerAuth fallback)."
    - "A new ArangoSearch view customer360_chunks_search_view exists on customer360_Chunks.content with the text_en analyzer, and BM25 over it returns chunk _ids."
    - "A spike confirms vector(Chunks) + BM25(new Chunks view) fused by _id in TS traverses PART_OF to Document.account_id and returns sourced Meridian sentiment chunks."
    - "ANTHROPIC_API_KEY is present in .env."
  artifacts:
    - path: "agent/package.json"
      provides: "TS workspace with pinned deps + test/typecheck scripts"
      contains: "\"ai\": \"6.0.208\""
    - path: "agent/src/envelope.ts"
      provides: "The shared Zod EnvelopeSchema contract (D-03a) consumed by Phases 5/6/7"
      exports: ["EnvelopeSchema", "CitationSchema", "ClaimSchema", "RetrievalPathFragment", "Envelope"]
      min_lines: 30
    - path: "agent/src/db.ts"
      provides: "arangojs Database singleton (Node runtime, basic auth + JWT fallback)"
      exports: ["db", "getDb"]
    - path: "agent/src/retrievalPath.ts"
      provides: "RetrievalPathFragment type + merge helper"
      exports: ["mergeRetrievalPaths"]
  key_links:
    - from: "agent/src/db.ts"
      to: "ArangoDB customer360"
      via: "new Database({url, databaseName, auth})"
      pattern: "new Database\\("
    - from: "agent/test/hybridSpike.test.ts"
      to: "customer360_Chunks + customer360_chunks_search_view"
      via: "APPROX_NEAR_COSINE + BM25 fused in TS, PART_OF traversal"
      pattern: "APPROX_NEAR_COSINE"
---

<objective>
Wave 0 — stand up the first TypeScript surface in this Python repo and de-risk the three RESEARCH-flagged landmines before any specialist tool is wired: (1) the chunk-level BM25 view that the hybrid specialist depends on, (2) arangojs auth against the live cluster, and (3) the Anthropic key prerequisite. Define the Zod envelope schema — the contract Phases 5/6/7 all consume — as a first-class shared artifact, and prove the hybrid RRF retrieval path returns sourced chunks before tasks that build on it are planned.

Purpose: Everything in Waves 1 and 2 depends on these prerequisites existing and being proven. Wave 0 gates the rest of the phase (RESEARCH §Wave 0 Gaps, VALIDATION Wave 0 Requirements).
Output: agent/ TS package (deps installed, vitest configured), agent/src/envelope.ts (the shared contract), agent/src/db.ts, agent/src/retrievalPath.ts, the customer360_chunks_search_view DDL applied, ANTHROPIC_API_KEY in .env, and three passing spike/unit tests (envelope schema, arangojs read, hybrid RRF sourced retrieval).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-custom-reasoning-agent/05-RESEARCH.md
@.planning/phases/05-custom-reasoning-agent/05-CONTEXT.md
@.planning/phases/05-custom-reasoning-agent/05-VALIDATION.md
@CLAUDE.md
@docs/research/arango-capability-check.md
@scripts/stamp_account_id.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify ANTHROPIC_API_KEY is present in .env</name>
  <files>.env, .env.example, .gitignore</files>
  <read_first>
    - .env (the file being modified — currently has ARANGO_* + OPENAI_API_KEY, NO ANTHROPIC_API_KEY)
    - .env.example (mirror the new var here)
    - .planning/phases/05-custom-reasoning-agent/05-RESEARCH.md §"Common Pitfalls" Pitfall 2 + §"Environment Availability" (ANTHROPIC_API_KEY missing, no fallback — blocks the planner)
  </read_first>
  <what-built>
    The planner (Opus 4.8) and cheaper sub-step models (Sonnet 4.6 / Haiku 4.5) are instantiated via @ai-sdk/anthropic, which reads ANTHROPIC_API_KEY from the environment (x-api-key header). The repo .env currently contains only OPENAI_API_KEY, so the first agent.generate call would 401. Add ANTHROPIC_API_KEY to .env and a placeholder line to .env.example.
  </what-built>
  <how-to-verify>
    1. Obtain an Anthropic API key from https://console.anthropic.com/settings/keys
    2. Add a line `ANTHROPIC_API_KEY=sk-ant-...` to /Users/plosiewicz/Desktop/customer360/.env
    3. Add a placeholder line `ANTHROPIC_API_KEY=` to /Users/plosiewicz/Desktop/customer360/.env.example
    4. Confirm .env is still gitignored (it is) so the secret never commits.
  </how-to-verify>
  <acceptance_criteria>
    - `grep -q '^ANTHROPIC_API_KEY=.\+' .env` exits 0 (the key has a value in .env).
    - `grep -q '^ANTHROPIC_API_KEY=' .env.example` exits 0 (placeholder present).
    - `git check-ignore .env` prints `.env` (still ignored — secret not committed).
  </acceptance_criteria>
  <action>Add ANTHROPIC_API_KEY to .env with a real value from the Anthropic Console, add a placeholder ANTHROPIC_API_KEY= line to .env.example, and confirm .env remains gitignored. This is a human-action checkpoint because the API key is a secret only the user can supply (RESEARCH Pitfall 2 / Environment Availability — no fallback). @ai-sdk/anthropic reads the key from env automatically; no code change is needed, only the secret.</action>
  <verify>grep -q '^ANTHROPIC_API_KEY=.\+' .env && grep -q '^ANTHROPIC_API_KEY=' .env.example && git check-ignore .env</verify>
  <done>ANTHROPIC_API_KEY has a value in .env, a placeholder exists in .env.example, and .env is still gitignored (secret never committed).</done>
  <resume-signal>Type "approved" once ANTHROPIC_API_KEY is in .env, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold the agent/ TypeScript package + Zod envelope contract + db singleton</name>
  <files>agent/package.json, agent/tsconfig.json, agent/vitest.config.ts, agent/src/envelope.ts, agent/src/db.ts, agent/src/retrievalPath.ts, agent/test/envelope.test.ts, agent/test/fixtures.ts, .gitignore</files>
  <read_first>
    - .planning/phases/05-custom-reasoning-agent/05-RESEARCH.md §"Standard Stack" (exact version pins + install command), §"Recommended Project Structure" (the agent/ layout), §"Code Examples" (the EnvelopeSchema + arangojs db singleton source), §"State of the Art" (Zod 4, Output.object stable, do not mix Zod majors)
    - package.json (the existing slopcheck-probe at repo root — confirms the same pins; the real package goes in agent/, do NOT reuse the probe)
    - scripts/stamp_account_id.py (the established .env var names ARANGO_ENDPOINT/DATABASE/USERNAME/PASSWORD and the dotenv-override discipline to mirror)
    - .gitignore (the file being modified — add agent/node_modules)
  </read_first>
  <action>
    Create the agent/ npm package (the first TS surface; sibling to scripts/). Write agent/package.json with type "module", a "test" script `vitest run`, a "typecheck" script `tsc --noEmit`, and the exact pinned deps from RESEARCH §Standard Stack: dependencies ai 6.0.208, @ai-sdk/anthropic 3.0.85, @ai-sdk/openai 3.0.73, arangojs 10.3.1, zod 4.4.3, dotenv; devDependencies typescript 6.0.3, tsx 4.22.4, vitest 4.1.9, @types/node. Do NOT install ai@7 (beta). Write agent/tsconfig.json targeting Node (module/moduleResolution NodeNext, target ES2022, strict true). Write agent/vitest.config.ts (globals, node environment). Run `cd agent && npm install` to produce agent/package-lock.json and agent/node_modules; add `agent/node_modules` to .gitignore.
    Write agent/src/envelope.ts EXACTLY as the shared contract (D-03a, RESEARCH §Code Examples): export CitationSchema {graph: enum[structured,unstructured], collection: string, _id: string, aql: string, traversal: string optional}, ClaimSchema {text, citations: CitationSchema[]}, RetrievalPathFragment {graph, collection, _ids: string[], query: string}, EnvelopeSchema {answer, refused: boolean default false, claims: ClaimSchema[], citations: CitationSchema[], retrievalPath: RetrievalPathFragment[], reasoningTrace: string[]}, and the inferred Envelope type. This schema is imported unchanged by Waves 1-2 and by Phases 6/7 — define it once here.
    Write agent/src/retrievalPath.ts: a mergeRetrievalPaths(fragments) helper that flattens/dedupes RetrievalPathFragment[] for the envelope (used by the planner in Wave 2).
    Write agent/src/db.ts: the arangojs Database singleton per RESEARCH §Code Examples — new Database({url: ARANGO_ENDPOINT, databaseName: ARANGO_DATABASE ?? 'customer360', auth: {username: ARANGO_USERNAME, password: ARANGO_PASSWORD}}). Export both `db` (the singleton) and `getDb()` (factory, so tests can construct with the JWT fallback if Task 3 finds basic-auth reads 401). Load env via dotenv with override:true at the top of the CLI/test entrypoints (mirror the Python load_dotenv(override=True) rule — RESEARCH Pitfall 3) — do NOT load dotenv inside db.ts module scope where Next.js (Phase 6) loads its own env; expose a small loadEnv() helper instead and call it from test/CLI entrypoints.
    Write agent/test/fixtures.ts: export the two fixed account_ids — NORTHWIND_ACCOUNT_ID = '0d5b5863-d3da-51e3-b117-ddbfa7ba2d16' and MERIDIAN_ACCOUNT_ID = '9eff6d7b-7311-5525-be75-5b82a855ece7' (from scripts/demo_critical.py) — plus a hasLiveDb() guard returning true only when ARANGO_ENDPOINT + ARANGO_PASSWORD are set (the skip-guard for live-DB tests; VALIDATION Wave 0).
    Write agent/test/envelope.test.ts: unit tests (no live DB) asserting EnvelopeSchema.parse accepts a well-formed envelope (a claim with ≥1 citation carrying a real-looking _id) and .safeParse rejects a malformed one (e.g. citation missing _id, or graph not in the enum).
  </action>
  <verify>
    <automated>cd agent && npm install && npx vitest run test/envelope.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && grep -q '"ai": "6.0.208"' package.json` exits 0 (exact pin, not ai@7).
    - `cd agent && npx vitest run test/envelope.test.ts` exits 0 with the parse-valid and reject-malformed cases green.
    - `cd agent && npx tsc --noEmit` exits 0 (envelope.ts, db.ts, retrievalPath.ts typecheck clean).
    - `grep -q 'EnvelopeSchema' agent/src/envelope.ts && grep -q 'retrievalPath' agent/src/envelope.ts` exits 0.
    - `grep -q 'agent/node_modules' .gitignore` exits 0.
  </acceptance_criteria>
  <done>The agent/ package installs with exact pins, the Zod EnvelopeSchema is the single shared contract and round-trips a valid envelope while rejecting a malformed one, db.ts exports the arangojs singleton + getDb factory, and the whole package typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Apply the Chunks BM25 view DDL + arangojs read spike + hybrid RRF sourced-retrieval spike</name>
  <files>agent/test/db.spike.test.ts, agent/test/hybridSpike.test.ts, agent/src/db.ts</files>
  <read_first>
    - .planning/phases/05-custom-reasoning-agent/05-RESEARCH.md §"Hybrid-retrieval landmine" (vector on Chunks, BM25 view on the WRONG collection — must create a view on Chunks.content), §"Architecture Patterns" Pattern 2 (the vector+BM25 shape), §"Common Pitfalls" Pitfall 6 (fuse the two _id lists in TS, NOT in AQL — no enumerate; score = Σ 1/(60+rank)), Pitfall 7 (basic-auth reads may 401; useBearerAuth via POST /_open/auth fallback), §"Open Questions" Q1 + Q3
    - docs/research/arango-capability-check.md §"Step 5" (the proven text_en ArangoSearch view config to mirror) + the JWT POST /_open/auth flow
    - scripts/stamp_account_id.py (the live-cluster DDL idempotency style + the bearer-token auth flow to port)
    - agent/src/db.ts (the file being modified — wire the JWT fallback here if the basic-auth read spike 401s)
    - agent/test/fixtures.ts (MERIDIAN_ACCOUNT_ID + hasLiveDb guard)
  </read_first>
  <action>
    Apply the build-time DDL creating the chunk-level BM25 view (RESEARCH Pitfall 1, Open Q1). Create an ArangoSearch view named customer360_chunks_search_view linked to customer360_Chunks on the content field with the text_en analyzer, mirroring the proven config in arango-capability-check.md Step 5. Make the create idempotent (skip/replace if it already exists, mirroring the scripts/ DDL idempotency style). Apply it via a setup step in the spike test (or a small one-shot in agent/, executed once) using the bearer-token flow — DDL on this cluster requires a JWT (POST /_open/auth), per the Phase 1 finding and scripts/stamp_account_id.py. Wire that JWT-acquisition path into agent/src/db.ts getDb() as the documented fallback.
    Write agent/test/db.spike.test.ts: the ~10-min arangojs read spike (RESEARCH Q3). With hasLiveDb() guarding (skip when env absent), run `db.query(aql\`RETURN 1\`)` under basic auth; assert it returns [1]. If basic-auth reads 401 in practice, the test must instead exercise getDb() with useBearerAuth(jwt) and assert that path returns [1]. Document the working auth mode in the test so Wave 1 knows which path the specialists use.
    Write agent/test/hybridSpike.test.ts: the key hybrid RRF spike (RESEARCH Open Q1, the single highest-value spike of the phase). With hasLiveDb() guarding: (1) embed a Meridian sentiment query (e.g. "partnership health escalation ops burden") via OpenAI text-embedding-3-small dimensions=512 to match the 512-dim index; (2) run an APPROX_NEAR_COSINE query over customer360_Chunks.embedding returning the top ~32 chunk _ids with their order; (3) run a BM25 SEARCH query over customer360_chunks_search_view (TOKENS(query, "text_en")) returning the top ~32 chunk _ids with their order; (4) fuse the two ranked _id lists in TypeScript by RRF (score = Σ 1/(60+rank)) — NOT in AQL; (5) for the fused top-k, traverse 1..1 OUTBOUND PART_OF over customer360_Relations (filter IS_SAME_COLLECTION customer360_Documents) scoped by Document.account_id == MERIDIAN_ACCOUNT_ID, returning {chunk_id, content, account_id, citable_url, file_name}. Assert the result is non-empty, every row has account_id == MERIDIAN_ACCOUNT_ID, and at least one chunk content mentions a RED sentiment signal (escalation / ops burden / partnership health). This proves the fused+sourced retrieval path before the hybridRetrieve tool is planned (05-03).
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/db.spike.test.ts test/hybridSpike.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/db.spike.test.ts` exits 0 — the arangojs read returns [1] under the working auth mode (test documents which: basic or bearer).
    - The view exists: an AQL `FOR v IN customer360_chunks_search_view SEARCH ANALYZER(v.content IN TOKENS("escalation", "text_en"), "text_en") LIMIT 1 RETURN v._id` returns ≥0 rows without a "view not found" / "no such analyzer" error (asserted inside hybridSpike.test.ts).
    - `cd agent && npx vitest run test/hybridSpike.test.ts` exits 0 — the fused, account-scoped, PART_OF-traversed result for MERIDIAN_ACCOUNT_ID is non-empty and every row's account_id equals MERIDIAN_ACCOUNT_ID.
    - `grep -q 'APPROX_NEAR_COSINE' agent/test/hybridSpike.test.ts && grep -q 'customer360_chunks_search_view' agent/test/hybridSpike.test.ts` exits 0.
    - `grep -vE '^\s*(//|\*)' agent/test/hybridSpike.test.ts | grep -c 'BM25' | grep -qv '^0$'` — RRF spike fuses a real BM25 list (not just a comment).
  </acceptance_criteria>
  <done>The customer360_chunks_search_view exists on Chunks.content with text_en; an arangojs read succeeds against the live cluster under a documented auth mode (basic or JWT fallback wired into db.ts); and the vector+BM25 RRF fusion (fused in TS) traverses PART_OF to return sourced Meridian RED-sentiment chunks scoped to the Meridian account — the hybrid path is proven before 05-03 builds the tool on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent code → ArangoDB | All DB access is read-only AQL; the only write in this phase is the one-time idempotent view DDL (build-time, not on the query path). |
| .env secrets → process | ANTHROPIC_API_KEY / ARANGO_PASSWORD live in gitignored .env; never serialized into test output, logs, or any artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Info Disclosure | .env (ANTHROPIC_API_KEY, ARANGO_PASSWORD) | mitigate | .env stays gitignored (verified in Task 1 via git check-ignore); .env.example carries only placeholders; no key value printed by any test. |
| T-05-02 | Tampering | view DDL on the live shared cluster | mitigate | The view create is idempotent (skip/replace if exists) and read-only-additive (a new ArangoSearch view; no existing collection mutated); applied via the JWT flow the cluster requires for DDL. |
| T-05-03 | Spoofing/Auth | arangojs connection to prod cluster | mitigate | Basic-auth read spike first; if 401, fall back to the verified POST /_open/auth → useBearerAuth(jwt) flow ported from scripts (Phase 1 finding); no credentials hardcoded. |
| T-05-SC | Tampering | npm installs (ai/@ai-sdk/*/arangojs/zod/vitest/tsx/typescript) | mitigate | All packages [OK] in RESEARCH §Package Legitimacy Audit (slopcheck 0.6.1); vitest [SUS] is a documented false positive (official Vite-team runner, repo verified); exact versions pinned; no postinstall scripts (RESEARCH verified). No [ASSUMED]/[SLOP] packages → no blocking checkpoint required. |
</threat_model>

<verification>
- `cd agent && npm install` succeeds with the exact pins (ai@6.0.208, never ai@7).
- `cd agent && npx vitest run` (unit + spikes) exits 0; live-DB spikes self-skip cleanly when .env is absent (hasLiveDb guard).
- `cd agent && npx tsc --noEmit` exits 0.
- ANTHROPIC_API_KEY present in .env (Task 1 acceptance), .env still gitignored.
- customer360_chunks_search_view exists and BM25 over it returns chunk _ids; the RRF-fused, account-scoped, PART_OF-traversed Meridian retrieval is non-empty and correctly sourced.
</verification>

<success_criteria>
- The agent/ TS package exists, installs with pinned deps, and typechecks.
- agent/src/envelope.ts is the single shared Zod contract (EnvelopeSchema + sub-schemas + Envelope type), proven by a passing parse/reject unit test.
- The three RESEARCH landmines are closed: ANTHROPIC_API_KEY present, arangojs reads work against the live cluster (auth mode documented), and the chunk-level BM25 view exists with the hybrid RRF path proven to return sourced Meridian RED chunks.
- Wave 1 (05-02, 05-03) and Wave 2 (05-04) can now build on a known-good envelope schema, db singleton, and hybrid retrieval shape.
</success_criteria>

<output>
Create `.planning/phases/05-custom-reasoning-agent/05-01-SUMMARY.md` when done — record the working arangojs auth mode (basic vs bearer), the exact view name/analyzer applied, and a one-line confirmation the hybrid spike returned sourced Meridian RED chunks (so 05-03 inherits the proven query shape).
</output>
