---
phase: 05-custom-reasoning-agent
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - agent/src/tools/structuredQuery.ts
  - agent/src/tools/bridgeResolve.ts
  - agent/test/structuredQuery.test.ts
  - agent/test/bridgeResolve.test.ts
autonomous: true
requirements: [AGENT-03]

must_haves:
  truths:
    - "structuredQuery returns {data, retrievalPath} for facets usage, contract, nps, contact, opportunity, account against the live structured graph, scoped by account_id."
    - "bridgeResolve resolves a canonical entity_id to its structured leaves AND KG entities across the same_as bridge and returns {data, retrievalPath}."
    - "bridgeResolve keys on the structured-side same_as edge (works for all 9 demo-critical ids), NOT the partial KG entity_id stamp (only ~3 of 9)."
    - "Every AQL in both specialists is read-only, parameterized via the aql tag with literal collection names, and LIMIT-bounded."
    - "Each tool's retrievalPath fragment carries {graph, collection, _ids, query} with the real ArangoDB _ids returned."
  artifacts:
    - path: "agent/src/tools/structuredQuery.ts"
      provides: "Specialist 1 — curated named-graph AQL over customer360_structured (one query per facet)"
      exports: ["structuredQuery"]
      min_lines: 60
    - path: "agent/src/tools/bridgeResolve.ts"
      provides: "Specialist 3 — same_as bridge traversal ported from verify_entity_bridge.py::probe_trace"
      exports: ["bridgeResolve"]
      min_lines: 30
  key_links:
    - from: "agent/src/tools/structuredQuery.ts"
      to: "customer360_structured (UsageFact, Contract, NPS, Contact, Opportunity, Account)"
      via: "db.query(aql`...`) read-only, LIMIT-bounded, FILTER account_id"
      pattern: "aql`"
    - from: "agent/src/tools/bridgeResolve.ts"
      to: "canonical_entities + same_as"
      via: "WITH-declared INBOUND same_as traversal"
      pattern: "same_as"
---

<objective>
Wave 1 (parallel with 05-03) — build two of the three curated-AQL specialist tools (D-04): structuredQuery (named-graph traversals over the structured CRM/Snowflake/DocuSign graph) and bridgeResolve (the Phase-4 same_as cross-graph entity bridge). Each is an AI SDK `tool` with a Zod inputSchema and an execute that runs read-only, parameterized, LIMIT-bounded AQL and returns {data, retrievalPath}. Together they cover the structured + resolver halves of the 6 locked question patterns (AGENT-03).

Purpose: These two specialists hold the deterministic structured backbone the planner composes (Wave 2). bridgeResolve must wrap the verified Phase-4 traversal exactly, keying on the structured-side same_as edge (all 9 demo-critical entities have one) — not the partial KG stamp.
Output: agent/src/tools/structuredQuery.ts, agent/src/tools/bridgeResolve.ts, and their integration tests (live-DB, skip-guarded).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-custom-reasoning-agent/05-RESEARCH.md
@.planning/phases/05-custom-reasoning-agent/05-CONTEXT.md
@CLAUDE.md
@docs/research/locked-questions-and-data-map.md
@scripts/verify_entity_bridge.py
@scripts/demo_critical.py
@.planning/phases/05-custom-reasoning-agent/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: structuredQuery specialist — curated named-graph AQL per facet</name>
  <files>agent/src/tools/structuredQuery.ts, agent/test/structuredQuery.test.ts</files>
  <behavior>
    - facet 'usage' for MERIDIAN_ACCOUNT_ID returns UsageFact rows (edition, query_volume_m, cluster_nodes, graphrag_enabled, period) with real _ids, sorted period DESC, LIMIT-bounded.
    - facet 'nps' returns NPS rows including score/nps_score (the GREEN numeric) AND verbatim_sentiment (the free-text that can read RED) — both fields present (critical for Q12/Q2).
    - facet 'contract' returns Contract rows (value_usd, renewal_date, days_to_renewal, auto_renew, status, product_scope).
    - facet 'opportunity' returns Opportunity rows (amount_usd, stage, opportunity_type, product_scope, close_date).
    - facet 'contact' returns Contact rows (full_name, role, title, email, active_from).
    - facet 'account' returns the Account row (account_name, segment, health_score, products_contracted, deployment_date).
    - Every returned retrievalPath has graph 'structured', the correct collection, _ids matching data[]._id, and a query string.
    - Passing an unknown facet is rejected by the Zod enum before any AQL runs.
  </behavior>
  <read_first>
    - .planning/phases/05-custom-reasoning-agent/05-RESEARCH.md §"Architecture Patterns" Pattern 1 (the tool() shape, one hardcoded query per facet, literal collection names — NEVER interpolate a collection from input), §"Live Data Layer" structured-graph table (exact verified collection + field names + counts), §"Curated AQL Map" (which facets each of the 6 questions needs), §"Anti-Patterns to Avoid", §"Security Domain" (read-only, aql tag, LIMIT)
    - docs/research/locked-questions-and-data-map.md (the 6 questions' structured records — Q7 usage/contract/opportunity, Q2 contract/usage/nps, Q12 usage/nps-score, Q9 contact, Q5 account/usage, Q8 contract/opportunity/usage)
    - agent/src/db.ts (the db singleton + working auth mode from 05-01), agent/src/envelope.ts (RetrievalPathFragment shape), agent/test/fixtures.ts (account_id fixtures + hasLiveDb guard)
    - .planning/phases/05-custom-reasoning-agent/05-01-SUMMARY.md (working arangojs auth mode)
  </read_first>
  <action>
    Define structuredQuery as an AI SDK `tool` (import tool from 'ai'). inputSchema (Zod): {accountId: string (the canonical account entity_id == Account._key), facet: enum['usage','contract','nps','contact','opportunity','account']}. execute switches on facet to ONE curated, read-only AQL per facet via the arangojs `aql` template tag — collection names are STRING LITERALS inside the template (UsageFact, Contract, NPS, Contact, Opportunity, Account), accountId is the only bind value (${accountId}), every query FILTERs on account_id and is LIMIT-bounded (e.g. LIMIT 12 for usage/contract, LIMIT 30 for nps, LIMIT 1 for account). Use the live-verified field names from RESEARCH §Live Data Layer — for 'nps' MUST return both the numeric score/nps_score AND verbatim_sentiment (the green-vs-red split). Each branch returns {data, retrievalPath: {graph:'structured', collection:<the literal>, _ids: data.map(d=>d._id), query:<a short human-readable AQL summary string>}}.
    NEVER interpolate the collection or field name from input (RESEARCH Anti-Patterns / Security Domain — reopens the injection surface CLAUDE.md forbids). The facet enum is the only routing; no generated AQL.
    Write agent/test/structuredQuery.test.ts: integration tests (hasLiveDb-guarded) exercising each facet against MERIDIAN_ACCOUNT_ID and NORTHWIND_ACCOUNT_ID, asserting non-empty data, _ids present, retrievalPath.graph === 'structured', and for 'nps' that both score and verbatim_sentiment keys appear. Include one unit assertion that the Zod inputSchema rejects an unknown facet.
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/structuredQuery.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/structuredQuery.test.ts` exits 0 (all facets return sourced rows; nps returns both score and verbatim_sentiment; unknown facet rejected).
    - `grep -c "aql\`" agent/src/tools/structuredQuery.ts` ≥ 6 (one curated query per facet).
    - `grep -vE '^\s*(//|\*)' agent/src/tools/structuredQuery.ts | grep -E 'IN \$\{|COLLECTION\(\$\{|_collection' | grep -qv .` — no collection-name interpolation from input (grep finds nothing → exits 1; the gate passes when this is empty).
    - `grep -cE 'LIMIT [0-9]+' agent/src/tools/structuredQuery.ts` ≥ 6 (every facet query LIMIT-bounded).
    - `cd agent && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>structuredQuery returns sourced {data, retrievalPath} for all six facets against the live structured graph, scoped by account_id, with the NPS green/red split exposed; all AQL is read-only, literal-collection, LIMIT-bounded, and bind-parameterized; unknown facets are rejected by Zod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bridgeResolve specialist — same_as cross-graph traversal (ported from probe_trace)</name>
  <files>agent/src/tools/bridgeResolve.ts, agent/test/bridgeResolve.test.ts</files>
  <behavior>
    - bridgeResolve(entityId) for each of the 9 demo-critical ids returns a hub with canonical_id == entityId, its structured leaf nodes (Account/Contact/Contract), and any KG entities.
    - For an id whose KG entity_id stamp is absent (~6 of 9), the structured leaves are STILL returned (resolution keys on the structured-side same_as edge, not the KG stamp).
    - retrievalPath has graph 'structured', collection 'same_as', _ids = the resolved structured + KG node _ids, and a query summary.
    - An unknown/absent entityId returns an empty-but-valid result (data: []), not an error.
  </behavior>
  <read_first>
    - scripts/verify_entity_bridge.py::probe_trace (lines 451-561 — the EXACT traversal AQL to port verbatim, including the WITH canonical_entities, Account, Contract, Contact, customer360_Entities clause required for cluster mode) and check_demo_critical (the structured-side same_as edge is the authoritative "linked" signal; the KG stamp is reporting-only)
    - scripts/demo_critical.py (the 9 demo-critical ids + display names the test iterates)
    - .planning/phases/05-custom-reasoning-agent/05-RESEARCH.md §"Code Examples" bridgeResolve (the ported aql-tag version), §"Common Pitfalls" Pitfall 4 (key on the structured same_as edge — all 9 have one; the KG stamp covers only ~3) + Pitfall 5 (cluster traversals need WITH)
    - agent/src/db.ts, agent/src/envelope.ts (RetrievalPathFragment), agent/test/fixtures.ts
    - .planning/phases/04-canonical-entity-layer/04-VERIFICATION.md (what the bridge guarantees)
  </read_first>
  <action>
    Define bridgeResolve as an AI SDK `tool`. inputSchema (Zod): {entityId: string (the canonical_id == entity_id)}. execute runs ONE read-only AQL ported from verify_entity_bridge.py::probe_trace (RESEARCH §Code Examples bridgeResolve): WITH canonical_entities, Account, Contract, Contact, customer360_Entities (the WITH clause is mandatory for the clustered prod deployment — Pitfall 5); FOR hub IN canonical_entities FILTER hub.canonical_id == ${entityId}; collect structured leaves via 1..1 INBOUND hub._id same_as filtered to NOT IS_SAME_COLLECTION('customer360_Entities', leaf); collect KG nodes via the same traversal filtered to IS_SAME_COLLECTION('customer360_Entities', e); RETURN {canonical_id, display_name, entity_type, account_id, structured_nodes, kg_nodes}. entityId is the only bind value; all collection names are literals. Return {data, retrievalPath: {graph:'structured', collection:'same_as', _ids: data.flatMap(d=>[...structured_nodes,...kg_nodes].map(n=>n._id)), query:'canonical_entities ←same_as← {structured leaves, KG entities}'}}.
    The resolution MUST key on the structured-side same_as edge (Pitfall 4) so all 9 demo-critical entities resolve — do NOT gate on the KG entity_id stamp.
    Write agent/test/bridgeResolve.test.ts: integration test (hasLiveDb-guarded) iterating ALL 9 demo-critical ids from a local copy of the id list (or import the values into a TS fixture), asserting each returns a hub with matching canonical_id and ≥1 structured_node — including the ~6 ids that have NO KG stamp (proving structured-side keying). Add one case asserting an absent id returns data: [] without throwing.
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/bridgeResolve.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/bridgeResolve.test.ts` exits 0 — all 9 demo-critical ids resolve with ≥1 structured_node (including unstamped ids); absent id returns [] without error.
    - `grep -q 'WITH canonical_entities' agent/src/tools/bridgeResolve.ts` exits 0 (cluster WITH clause ported).
    - `grep -q 'same_as' agent/src/tools/bridgeResolve.ts` exits 0.
    - `grep -vE '^\s*(//|\*)' agent/src/tools/bridgeResolve.ts | grep -qi 'entity_id IN' && false || true` — resolution does not gate on the KG entity_id stamp (Pitfall 4); structured-side edge is the signal.
    - `cd agent && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>bridgeResolve wraps the verified Phase-4 traversal verbatim (WITH clause, structured-side same_as keying), resolves all 9 demo-critical entities to their structured leaves regardless of KG-stamp coverage, returns a valid empty result for absent ids, and emits a same_as retrievalPath fragment with the real node _ids.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent input (accountId/entityId from the planner, ultimately from a free-form question) → AQL | The only place untrusted text reaches the DB; must cross via the aql tag as a bind value, never as a collection/field name. |
| tool → ArangoDB | Read-only AQL, LIMIT-bounded, collection-whitelisted (literal names). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | structuredQuery / bridgeResolve AQL (injection via accountId/entityId) | mitigate | accountId/entityId bound via the arangojs `aql` tag (auto bind-parameter); facet routed by a Zod enum; collection + field names are string literals inside the template — never interpolated from input. Grep gate in acceptance criteria proves no collection interpolation. |
| T-05-05 | DoS | unbounded/expensive structured query | mitigate | Every curated query is LIMIT-bounded (acceptance criteria asserts ≥6 LIMIT clauses); no client-supplied LIMIT. |
| T-05-06 | Repudiation/Integrity | wrong-entity resolution (champion → unknown contact) | mitigate | bridgeResolve keys on the structured-side same_as edge (all 9 demo-critical entities have one), not the partial KG stamp (Pitfall 4); test asserts all 9 resolve. |
</threat_model>

<verification>
- `cd agent && npx vitest run test/structuredQuery.test.ts test/bridgeResolve.test.ts` exits 0 (live-DB, skip-guarded when env absent).
- `cd agent && npx tsc --noEmit` exits 0.
- No collection/field-name interpolation from input in either tool (grep gates).
- Both tools return {data, retrievalPath} with real _ids; NPS exposes the green/red split; all 9 demo-critical entities resolve.
</verification>

<success_criteria>
- structuredQuery covers the structured halves of all 6 question patterns (AGENT-03) via one curated, read-only, LIMIT-bounded AQL per facet.
- bridgeResolve wraps the verified Phase-4 same_as bridge, resolving all 9 demo-critical entities via the structured-side edge.
- Both tools emit a correct retrievalPath fragment and pass their live-DB integration tests; the package typechecks.
- The planner (Wave 2) can compose these two specialists alongside hybridRetrieve (05-03).
</success_criteria>

<output>
Create `.planning/phases/05-custom-reasoning-agent/05-02-SUMMARY.md` when done — list the structuredQuery facet→collection map and confirm all 9 demo-critical ids resolve via bridgeResolve (so 05-04's planner prompt can reference the available facets/resolver).
</output>
