---
phase: 05-custom-reasoning-agent
plan: 04
type: execute
wave: 2
depends_on: ["05-02", "05-03"]
files_modified:
  - agent/src/grounding.ts
  - agent/src/agent.ts
  - agent/src/index.ts
  - agent/cli.ts
  - agent/test/grounding.test.ts
  - agent/test/questions.eval.test.ts
autonomous: true
requirements: [AGENT-01, AGENT-02, AGENT-05, AGENT-07]

must_haves:
  truths:
    - "askQuestion(question) returns a Zod-validated {answer, refused, claims[], citations[], retrievalPath[], reasoningTrace[]} envelope for any free-form question."
    - "The ToolLoopAgent planner (OpenAI flagship via @ai-sdk/openai, D-06) decomposes a question and composes the three specialists (structuredQuery, hybridRetrieve, bridgeResolve), merging their retrievalPath fragments."
    - "For the 6 locked questions, the envelope is non-refusal and cites the expected graphs (Q7 structured-only; Q2/Q5/Q8/Q9/Q12 cite ≥1 structured AND ≥1 unstructured _id)."
    - "Q12 names the contradiction ('usage green but sentiment red') and cites ≥1 structured AND ≥1 unstructured _id (code-asserted, D-05)."
    - "Grounding is verified IN CODE: every claim citation _id must be in the set of _ids the tools actually returned; an out-of-scope question returns refused:true with partial citations and no fabricated _id (D-02, never model self-check, provider-independent)."
    - "A CLI harness (tsx agent/cli.ts \"question\") runs the agent headlessly and prints the envelope (D-01)."
  artifacts:
    - path: "agent/src/grounding.ts"
      provides: "Code-level grounding gate → structured refusal (D-02); reconciliation citation assertion (D-05). Provider-independent — pure code, no model call."
      exports: ["enforceGrounding", "assertReconciliation"]
      min_lines: 30
    - path: "agent/src/agent.ts"
      provides: "ToolLoopAgent assembly (OpenAI provider, D-06) + planner system prompt + reconciliation directive + Output.object synthesis + PLANNER_MODEL config constant"
      exports: ["runAgent", "PLANNER_MODEL"]
      min_lines: 50
    - path: "agent/src/index.ts"
      provides: "Public askQuestion(question): Promise<Envelope> entrypoint (imported by Phases 6/7)"
      exports: ["askQuestion"]
    - path: "agent/cli.ts"
      provides: "Thin CLI harness (D-01) — tsx agent/cli.ts \"question\""
  key_links:
    - from: "agent/src/agent.ts"
      to: "structuredQuery + hybridRetrieve + bridgeResolve"
      via: "ToolLoopAgent tools: {...}"
      pattern: "ToolLoopAgent"
    - from: "agent/src/index.ts"
      to: "agent/src/grounding.ts"
      via: "enforceGrounding(envelope, returnedIds)"
      pattern: "enforceGrounding"
    - from: "agent/src/grounding.ts"
      to: "the set of tool-returned _ids"
      via: "code check: every citation._id ∈ returnedIds, else refuse"
      pattern: "refused"
---

<objective>
Wave 2 — assemble the headline deliverable: the ToolLoopAgent planner (OpenAI flagship via @ai-sdk/openai, D-06) that decomposes a free-form question, composes the three Wave-1 specialists, performs the Q12 cross-graph reconciliation as an explicit step (D-05), synthesizes the Zod-validated envelope via Output.object, and runs the code-level grounding gate that turns an ungrounded answer into a structured refusal (D-02). Expose it as askQuestion() + a thin CLI harness (D-01), and prove it against the 6 locked questions. Covers AGENT-01, AGENT-02, AGENT-05, AGENT-07.

Purpose: This is the composition + grounding layer — the phase's core value. Grounding/refusal is verified IN CODE (every citation _id checked against the set the tools actually returned), never by asking the model to self-verify (that reopens the hallucination surface the project exists to avoid) — this gate is provider-independent and unchanged by the OpenAI swap.
Output: agent/src/grounding.ts, agent/src/agent.ts, agent/src/index.ts (askQuestion), agent/cli.ts, and the grounding unit tests + the 6-question eval (which Phase 7 reuses).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-custom-reasoning-agent/05-RESEARCH.md
@.planning/phases/05-custom-reasoning-agent/05-CONTEXT.md
@.planning/phases/05-custom-reasoning-agent/05-VALIDATION.md
@CLAUDE.md
@docs/research/locked-questions-and-data-map.md
@.planning/phases/05-custom-reasoning-agent/05-01-SUMMARY.md
@.planning/phases/05-custom-reasoning-agent/05-02-SUMMARY.md
@.planning/phases/05-custom-reasoning-agent/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Code-level grounding gate + reconciliation assertion (D-02, D-05)</name>
  <files>agent/src/grounding.ts, agent/test/grounding.test.ts</files>
  <behavior>
    - enforceGrounding(envelope, returnedIds: Set<string>): if every claim has ≥1 citation AND every citation._id ∈ returnedIds → return the envelope unchanged. If any claim has no citation, or any citation._id ∉ returnedIds → return a refusal envelope (refused:true, answer = structured "cannot answer X because Y", keeping only the citations whose _id ∈ returnedIds as partial sourcing).
    - The refusal NEVER fabricates an _id and NEVER asks the model to re-judge — it is a pure code comparison against returnedIds (provider-independent; unaffected by the OpenAI swap).
    - assertReconciliation(envelope): true iff the citations include ≥1 graph 'structured' AND ≥1 graph 'unstructured' _id (the Q12/dual-graph signal, D-05).
    - A fully-grounded envelope with citations from both graphs passes both.
    - An envelope with a hallucinated citation _id (not in returnedIds) is converted to a refusal.
  </behavior>
  <read_first>
    - .planning/phases/05-custom-reasoning-agent/05-RESEARCH.md §"Architecture Patterns" Pattern 4 (grounding-driven refusal — verify _ids in CODE against the tool-returned set, NOT model self-check), §"Open Questions" Q2 (do both: prompt directive + a code assertion for reconciliation), §"Security Domain" (confident-wrong is the central control)
    - .planning/phases/05-custom-reasoning-agent/05-CONTEXT.md D-02 (refusal returns the same envelope, cites whatever WAS found; grounding drives the decision) + D-05 (reconciliation cites both graphs) + D-06 (the grounding gate is code-level and provider-independent — explicitly unchanged by the OpenAI swap)
    - agent/src/envelope.ts (EnvelopeSchema — the shape enforceGrounding operates on)
  </read_first>
  <action>
    Write agent/src/grounding.ts. Export enforceGrounding(envelope: Envelope, returnedIds: Set<string>): Envelope — the D-02 code gate. Build the supported-id set from returnedIds (every _id any tool actually returned during the loop). Compute ungrounded = envelope.citations.filter(c => !returnedIds.has(c._id)) and unsupportedClaims = envelope.claims.filter(cl => cl.citations.length === 0 || cl.citations.some(c => !returnedIds.has(c._id))). If ungrounded.length > 0 OR unsupportedClaims.length > 0 → return a refusal envelope: refused:true, answer = a structured "cannot answer <question> because <the supporting record/graph for claim …> was not found in the retrieved records", claims pruned to the grounded subset, citations kept only where _id ∈ returnedIds (partial sourcing per D-02), retrievalPath + reasoningTrace preserved. Otherwise return the envelope unchanged. This is a PURE function over (envelope, returnedIds) — no model call, no I/O — so it is unit-testable and is the single anti-hallucination control. It is provider-independent: the OpenAI swap (D-06) does NOT touch this file.
    Export assertReconciliation(envelope: Envelope): boolean — true iff envelope.citations has ≥1 {graph:'structured'} AND ≥1 {graph:'unstructured'}; used by the planner's Q12 path and the eval (D-05, RESEARCH Q2).
    Write agent/test/grounding.test.ts: PURE unit tests (no live DB, no model) — (a) fully-grounded both-graph envelope passes enforceGrounding unchanged and assertReconciliation===true; (b) an envelope with a citation _id NOT in returnedIds becomes refused:true and drops that citation but keeps the grounded ones; (c) an envelope with a claim that has zero citations becomes refused:true; (d) a structured-only envelope (Q7) passes enforceGrounding but assertReconciliation===false. These run on every commit without env.
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/grounding.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/grounding.test.ts` exits 0 — all four grounding/refusal cases pass (pure, runs without env).
    - `grep -q 'returnedIds' agent/src/grounding.ts && grep -q 'refused' agent/src/grounding.ts` exits 0.
    - `grep -vE '^\s*(//|\*)' agent/src/grounding.ts | grep -qiE 'generateText|generateObject|anthropic\(|openai\(|\.generate\(' && false || true` — grounding.ts makes NO model call (no self-verify surface; the gate is pure code, provider-independent).
    - `grep -q 'assertReconciliation' agent/src/grounding.ts` exits 0.
    - `cd agent && npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>enforceGrounding is a pure code gate that converts any ungrounded/hallucinated-citation envelope into a structured refusal with partial sourcing, making zero model calls (provider-independent — unchanged by the D-06 OpenAI swap); assertReconciliation enforces the dual-graph citation requirement. Both are unit-proven without a live DB or model.</done>
</task>

<task type="auto">
  <name>Task 2: ToolLoopAgent planner (OpenAI provider) + reconciliation directive + synthesis + askQuestion/CLI</name>
  <files>agent/src/agent.ts, agent/src/index.ts, agent/cli.ts</files>
  <read_first>
    - .planning/phases/05-custom-reasoning-agent/05-RESEARCH.md §"Architecture Patterns" Pattern 1 (ToolLoopAgent assembly, stopWhen stepCountIs(12) — note the provider is now @ai-sdk/openai per D-06, NOT @ai-sdk/anthropic), Pattern 3 (the Q12 reconciliation system-prompt directive — retrieve BOTH the structured usage/NPS-score GREEN signal AND the unstructured sentiment RED signal, name the contradiction, cite a record from EACH graph; never report 'healthy' from structured alone), Pattern 4 (Output.object(EnvelopeSchema) synthesis then the code grounding gate), §"State of the Art" (output: Output.object — drop experimental_), §"Architectural Responsibility Map" (planner owns decomposition + reconciliation + synthesis; grounding is post-synthesis), §"Don't Hand-Roll"
    - .planning/phases/05-custom-reasoning-agent/05-CONTEXT.md D-06 (v1 planner/answerer runs on OpenAI via @ai-sdk/openai — createOpenAI/openai(modelId); planner/answerer = OpenAI current flagship for tool-use + structured output; a cheaper OpenAI model may serve routing/extraction sub-steps; do NOT hard-pin a possibly-stale model id — read it from a single named config constant the Wave-0 spike confirmed against the live key), D-01 (standalone module + thin CLI, no HTTP route), D-03 (per-claim citations + separate reasoningTrace), D-04 (planner composes the 3 specialists), D-05 (reconciliation is an explicit planner step)
    - CLAUDE.md Technology Stack (OpenAI listed as a first-class provider — gpt-4.1/gpt-4o class for tool-use; AI SDK makes Claude/OpenAI interchangeable, a one-line provider swap)
    - agent/src/tools/structuredQuery.ts, agent/src/tools/bridgeResolve.ts (from 05-02), agent/src/tools/hybridRetrieve.ts (from 05-03), agent/src/grounding.ts (Task 1), agent/src/envelope.ts, agent/src/retrievalPath.ts (mergeRetrievalPaths), agent/src/db.ts (loadEnv — dotenv override, D-06 stale-shell gotcha)
    - .planning/phases/05-custom-reasoning-agent/05-01-SUMMARY.md (the confirmed OpenAI model id from the Wave-0 spike + the loadEnv discipline), 05-02-SUMMARY.md + 05-03-SUMMARY.md (the available facets / resolver / RED signals to reference in the system prompt)
  </read_first>
  <action>
    Write agent/src/agent.ts. Import the OpenAI provider from '@ai-sdk/openai' (createOpenAI / openai) per D-06 — v1 runs on OpenAI, NOT @ai-sdk/anthropic (that import must NOT appear). Define and export a single named config constant PLANNER_MODEL holding the OpenAI flagship model id used for planning + answer synthesis (recommended default: 'gpt-4.1' — an OpenAI flagship tool-use + structured-output model per CLAUDE.md; this is the ONE place to change the id, and the Wave-0/05-01 spike should have confirmed the exact live id against the OPENAI_API_KEY — do NOT hard-pin a stale id scattered through the code). Optionally define a ROUTING_MODEL constant (e.g. a cheaper OpenAI model like 'gpt-4o-mini') for any routing/extraction sub-step; if prepareStep routes cheap sub-steps, use ROUTING_MODEL there. Assemble a ToolLoopAgent (import from 'ai') with model openai(PLANNER_MODEL), tools {structuredQuery, hybridRetrieve, bridgeResolve}, and stopWhen stepCountIs(12) (RESEARCH Pattern 1). Author PLANNER_SYSTEM_PROMPT: instruct the planner to decompose the question, resolve named people/accounts/contracts via bridgeResolve to a canonical entity_id (so structuredQuery/hybridRetrieve can scope by account_id), pull the structured facets and the unstructured chunks needed, and — for "is this account actually healthy / renewal-risk / reconciliation" questions — ALWAYS retrieve BOTH the structured usage/NPS-score (GREEN) AND the unstructured sentiment (RED) and explicitly name any contradiction, citing a record from EACH graph (Pattern 3, D-05). The prompt MUST require per-claim citations and a separate reasoning trace (D-03), and instruct: if the records needed to support a claim are absent, say so rather than guess (D-02). Export runAgent(question): run the loop, collect EVERY _id any tool returned during the loop into a returnedIds Set (read from the tool-result messages — this is the ground-truth set for grounding), synthesize the final envelope with output: Output.object({name:'Answer', schema: EnvelopeSchema}) (RESEARCH Pattern 4), merge the tool retrievalPath fragments via mergeRetrievalPaths, and return {envelope, returnedIds}.
    Write agent/src/index.ts: export askQuestion(question: string): Promise<Envelope> — call loadEnv() (dotenv override, so OPENAI_API_KEY comes from .env and never a stale shell value — D-06 gotcha), runAgent(question), then enforceGrounding(envelope, returnedIds) (Task 1) and return the (possibly-refusal) envelope. This is THE public entrypoint Phases 6/7 import (D-01); no HTTP route.
    Write agent/cli.ts: a thin harness — loadEnv(), read the question from process.argv, await askQuestion(question), print JSON.stringify(envelope, null, 2). Runnable as `tsx agent/cli.ts "question"`. Never print env/keys (Security Domain).
    Add an "ask" script to agent/package.json: `tsx cli.ts`.
  </action>
  <verify>
    <automated>cd agent && npx tsc --noEmit && node -e "require('child_process').execSync('grep -q ToolLoopAgent src/agent.ts && grep -q enforceGrounding src/index.ts && grep -q askQuestion cli.ts',{cwd:process.cwd()})"</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx tsc --noEmit` exits 0 (the full module — agent.ts, index.ts, cli.ts — typechecks against the Wave-1 tools + grounding gate).
    - `grep -q 'ToolLoopAgent' agent/src/agent.ts && grep -q 'stepCountIs' agent/src/agent.ts` exits 0.
    - `grep -q "@ai-sdk/openai" agent/src/agent.ts && grep -q 'PLANNER_MODEL' agent/src/agent.ts` exits 0 — the planner runs on the OpenAI provider via a single named model constant (D-06).
    - `grep -q '@ai-sdk/anthropic' agent/src/agent.ts && false || true` — the Anthropic provider is NOT imported in v1 (D-06).
    - `grep -q 'Output.object' agent/src/agent.ts && grep -q 'EnvelopeSchema' agent/src/agent.ts` exits 0 (Zod-validated structured synthesis).
    - `grep -q 'structuredQuery' agent/src/agent.ts && grep -q 'hybridRetrieve' agent/src/agent.ts && grep -q 'bridgeResolve' agent/src/agent.ts` exits 0 (all three specialists composed — AGENT-01).
    - `grep -q 'enforceGrounding' agent/src/index.ts && grep -q 'askQuestion' agent/src/index.ts && grep -q 'loadEnv' agent/src/index.ts` exits 0 (grounding gate + dotenv-override env load wired into the public entrypoint).
    - `grep -iE 'process.env|ARANGO_PASSWORD|OPENAI_API_KEY' agent/cli.ts | grep -iE 'console.log|print|stringify' && false || true` — the CLI never prints secrets.
  </acceptance_criteria>
  <done>The ToolLoopAgent planner runs on the OpenAI provider (D-06) via a single named PLANNER_MODEL constant (no hard-pinned stale id, no @ai-sdk/anthropic import), composes the three specialists, carries the Q12 reconciliation directive, synthesizes a Zod-validated envelope, and is exposed as askQuestion() + a thin CLI; loadEnv (dotenv override) feeds OPENAI_API_KEY from .env and the grounding gate is wired into the public entrypoint so every returned envelope is code-grounded. The full module typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: 6-locked-question eval + dual-graph + Q12 + refusal integration tests</name>
  <files>agent/test/questions.eval.test.ts</files>
  <read_first>
    - .planning/phases/05-custom-reasoning-agent/05-RESEARCH.md §"Validation Architecture" Phase-Requirements-to-Test-Map (the exact -t filters + assertions for AGENT-01/02/05/07), §"Curated AQL Map" (which specialists each Q exercises + the expected GREEN/RED records)
    - docs/research/locked-questions-and-data-map.md (the 6 questions Q7/Q2/Q12/Q9/Q5/Q8, their accounts A=Northwind/B=Meridian, and the dual-graph proofs — Q7 structured-only, the other five dual)
    - .planning/phases/05-custom-reasoning-agent/05-VALIDATION.md (sampling rate; the 6 questions return valid envelopes is the phase gate; faithfulness eval itself is Phase 7)
    - .planning/phases/05-custom-reasoning-agent/05-CONTEXT.md D-06 (v1 runs on OPENAI_API_KEY — the eval guard checks for the OpenAI key, not an Anthropic key)
    - agent/src/index.ts (askQuestion), agent/src/grounding.ts (assertReconciliation), agent/test/fixtures.ts (account ids + hasLiveDb guard), 05-02/05-03 SUMMARYs (available facets + RED signals)
  </read_first>
  <action>
    Write agent/test/questions.eval.test.ts: integration tests (hasLiveDb + OPENAI_API_KEY guarded per D-06 — skip cleanly when absent) driving askQuestion() headlessly for the 6 locked questions. Phrase each question naturally from the data map theme (exact buyer phrasing is Phase 6). Assertions, tagged for the RESEARCH -t filters:
    - Q7 (test name includes "Q7"): non-refusal envelope; every citation graph === 'structured' (the intentional structured-only anchor); claims have ≥1 citation each.
    - Q2/Q5/Q8/Q9 (each a named test, one tagged "dual-graph"): non-refusal; assertReconciliation(envelope) === true (≥1 structured AND ≥1 unstructured _id) — AGENT-01/02.
    - Q12 (test name includes "Q12"): non-refusal; assertReconciliation === true; the answer text names the contradiction (matches /green/i AND /red|risk|sentiment/i, or equivalent), citing ≥1 structured AND ≥1 unstructured _id for Meridian — AGENT-05.
    - refusal (test name includes "refusal"): an out-of-scope question (e.g. "what is the CEO's home address?" — no supporting record exists) returns refused:true, with no citation whose _id is fabricated (every returned citation _id is a real ArangoDB _id shape) and partial citations only — AGENT-07.
    Each test asserts EnvelopeSchema.safeParse(envelope).success === true (the contract holds). Allow generous per-test timeouts (live model + DB).
    Document in a comment that faithfulness/coherence grading is Phase 7; this eval only proves the envelope is well-formed, dual-graph where required, and refuses when ungrounded (VALIDATION Manual-Only note).
  </action>
  <verify>
    <automated>cd agent && npx vitest run test/questions.eval.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd agent && npx vitest run test/questions.eval.test.ts` exits 0 when .env + OPENAI_API_KEY are present (all 6 questions return valid envelopes; Q7 structured-only; Q2/Q5/Q8/Q9/Q12 dual-graph; Q12 names the contradiction; refusal case refused:true). Skips cleanly (no failure) when env absent.
    - `grep -q 'Q12' agent/test/questions.eval.test.ts && grep -q 'assertReconciliation' agent/test/questions.eval.test.ts` exits 0 (AGENT-05 covered).
    - `grep -qi 'refusal' agent/test/questions.eval.test.ts && grep -q 'refused' agent/test/questions.eval.test.ts` exits 0 (AGENT-07 covered).
    - `grep -cE 'askQuestion\(' agent/test/questions.eval.test.ts` ≥ 6 (one drive per locked question).
    - `grep -q 'safeParse' agent/test/questions.eval.test.ts` exits 0 (envelope contract asserted).
  </acceptance_criteria>
  <done>The 6 locked questions each return a valid, well-formed envelope through askQuestion(): Q7 cites the structured graph only, the five dual questions cite both graphs, Q12 names the usage-green/sentiment-red contradiction citing both sides, and an out-of-scope question refuses with partial sourcing and no fabricated _id — AGENT-01/02/05/07 demonstrated end-to-end and reusable by Phase 7. The eval guards on OPENAI_API_KEY (D-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| free-form question → planner → tools | The whole untrusted-input path; the planner may only call the three curated tools, each Zod-validated and read-only — no generated AQL (v1). |
| model-proposed citations → envelope | The model (OpenAI flagship, D-06) proposes citation _ids; CODE (enforceGrounding) verifies each against the set the tools actually returned before the envelope is trusted — provider-independent control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Repudiation/Integrity | confident-wrong answer (hallucinated facts/citations) | mitigate | enforceGrounding (Task 1) verifies every citation _id against the tool-returned set IN CODE; any mismatch → structured refusal. The model never self-certifies grounding (grep gate proves grounding.ts makes no model call). This control is provider-independent — unchanged by the OpenAI swap (D-06). This is the project's central control. |
| T-05-12 | Tampering | prompt injection → unsafe tool args / scope escape | mitigate | The planner can only invoke the three curated tools; each tool's Zod inputSchema constrains args (account_id strings, facet enum, bounded k); all tool AQL is read-only — injected instructions cannot reach a write or a non-whitelisted collection. The "no generated AQL (v1)" control is unaffected by the provider swap. |
| T-05-13 | DoS | unbounded agent loop | mitigate | stopWhen: stepCountIs(12) caps the ToolLoopAgent; each tool query is LIMIT-bounded (Wave 1). |
| T-05-14 | Info Disclosure | secrets (OPENAI_API_KEY, ARANGO_PASSWORD) in reasoningTrace / CLI output | mitigate | reasoningTrace + retrievalPath carry only _ids/collections/AQL text; the OPENAI_API_KEY is loaded via dotenv override from .env (never a stale shell, D-06 gotcha) and never printed; the CLI prints only the envelope JSON (grep gate proves no secret is printed). |
</threat_model>

<verification>
- `cd agent && npx vitest run` (full suite: envelope, db.spike, hybridSpike, structuredQuery, bridgeResolve, rrf, hybridRetrieve, grounding, questions.eval) — unit tests pass without env; live-DB + model tests pass when .env+OPENAI_API_KEY present (D-06), skip cleanly otherwise.
- `cd agent && npx tsc --noEmit` exits 0.
- `tsx agent/cli.ts "is Meridian actually healthy?"` prints a valid envelope naming the contradiction (manual demo smoke — VALIDATION Manual-Only note).
- The 6 locked questions return well-formed envelopes; Q12 reconciles; the out-of-scope case refuses.
</verification>

<success_criteria>
- AGENT-01: the planner (OpenAI provider, D-06) decomposes a question and composes the three specialists, merging their retrievalPath fragments (grep + dual-graph eval).
- AGENT-02: each of the 6 locked questions returns a non-refusal envelope visibly drawing on the expected graph(s).
- AGENT-05: Q12 names the usage-green/sentiment-red contradiction citing ≥1 structured AND ≥1 unstructured _id (assertReconciliation).
- AGENT-07: out-of-scope/unanswerable questions return a code-grounded structured refusal with partial sourcing and no fabricated _id.
- D-01/D-02/D-03/D-05/D-06 honored: standalone module + CLI, code-level (provider-independent) grounding refusal, per-claim sourcing + separate reasoning trace, explicit reconciliation step, planner/answerer on the OpenAI provider via a single named model constant.
</success_criteria>

<output>
Create `.planning/phases/05-custom-reasoning-agent/05-04-SUMMARY.md` when done — record the askQuestion() signature + envelope shape (the Phase 6/7 contract), the confirmed PLANNER_MODEL OpenAI id (D-06), confirm the 6-question eval status, and note the CLI invocation for the demo smoke.
</output>
